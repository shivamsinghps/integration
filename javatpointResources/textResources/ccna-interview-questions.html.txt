
div => [ 
 ]
h1 => [ CCNA Interview Questions ]
p => [  A list of top frequently asked  CCNA interview questions  and answers are given below. ]
h3 => [ 1) What is the difference between switch and hub? ]
th => [ Basis of Comparison ]
th => [ Hub ]
th => [ Switch ]
td => [ Description ]
td => [ Hub is a networking device that connects the multiple devices to a single network. ]
td => [ A switch is a control unit that turns the flow of electricity on or off in a circuit. ]
td => [ Layer ]
td => [ Hubs are used at the physical layer. ]
td => [ Switches are used at the data link layer. ]
td => [ Transmission type ]
td => [ Transmission type can be unicast, broadcast or multicast. ]
td => [ Initially, the transmission type is broadcast and then is unicast. ]
td => [ Ports ]
td => [ Hub has 4/12 ports. ]
td => [ The switch has 24/48 ports. ]
td => [ Transmission mode ]
td => [ Half duplex ]
td => [ Half/Full duplex. ]
td => [ Collisions ]
td => [ Collisions occur commonly in a Hub. ]
td => [ No collisions occur in a full duplex switch. ]
td => [ Address used for data transmission ]
td => [ Hub uses MAC address for data transmission. ]
td => [ The switch uses a MAC address for data transmission. ]
td => [ Data transmission form ]
td => [ Electrical signal is a data transmission form of a hub. ]
td => [ A Frame is a data transmission form of a switch. ]
h3 => [ 2) What is the difference between Switch and Router? ]
th => [ Basis of Comparison ]
th => [ Router ]
th => [ Switch ]
td => [ Description ]
td => [ It is a layer 3 device that connects the two different networks and identifies the network devices based on their IP addresses. ]
td => [ It is a layer 2 device and determines the network devices based on their MAC addresses. ]
td => [ Mode of transmission ]
td => [ Router transmits the data in the form of packets. ]
td => [ Switch transmits the data in the form of frames. ]
td => [ Address used ]
td => [ It uses an IP address for the data transmission. ]
td => [ It uses a MAC address to transmit the data. ]
td => [ Layer of OSI model ]
td => [ It uses Layer 3 OSI model and layer is the network layer. ]
td => [ It uses layer 2 OSI model and layer is the data link layer. ]
td => [ Table ]
td => [ It uses a routing table for routes to move to the destination IP. ]
td => [ It uses a Content address memory table for MAC addresses. ]
td => [ Network used ]
td => [ It is used for WAN and LAN networks. ]
td => [ It is used only for LAN networks. ]
td => [ Mode of transmission ]
td => [ Router is used in a full-duplex mode. ]
td => [ A switch is used in half as well as in a full-duplex mode. ]
h3 => [ 3) What are the advantages of using Switches? ]
strong => [ Advantages of using Switches: ]
li => [ Switches are used to receive a signal and create a frame out of the bits from that signal. The signals enable you to get access and read the destination address and after reading that it forward that frame to appropriate frame. So, switches are the significant part of the transmission. ]
h3 => [ 4) What is Routing? ]
li => [ Routing is a process of finding a path to transfer data from source to destination. ]
li => [ Routing can be performed in a variety of networks such as circuit switched networks and computer networks. ]
li => [ In packet switching networks, routing makes a decision that directs the packets from source to the destination. ]
li => [ Routing makes use of a routing table, which maintains the routes of various destinations. ]
strong => [ Types of routing: ]
strong => [ Static routing ]
strong => [ Dynamic routing ]
h3 => [ 5) What are Routers? ]
li => [ The devices known as Routers do the process of routing. Routers are the network layer devices. ]
li => [ The router is a networking device which is used to transfer the data across the networks, and that can be wired or wireless. ]
li => [ Routers use headers and routing table to determine the best route for forwarding the packets. ]
li => [ Router analyzes the data which is being sent over the network, changes how it is packaged and send it over the network. ]
strong => [ Examples of routers are: ]
strong => [ Brouter ]
strong => [ Core router ]
strong => [ Edge router ]
strong => [ Virtual router ]
strong => [ Wireless router ]
h3 => [ 6) What is the advantage of VLAN? ]
p => [ VLAN is a custom network which is created from one or more existing LAN's. VLAN facilitates you to create a collision domain by groups other than just physical location while in conventional LAN domains are always tied to physical location. ]
strong => [ Advantage of VLAN: ]
strong => [ Broadcast control ]
strong => [ Simplified administration ]
strong => [ Security ]
strong => [ LAN segmentation ]
strong => [ Dynamic VLANs ]
strong => [ Protocol-based VLANs ]
h3 => [ 7) What is HDLC? ]
li => [ HDLC stands for High-Level Data Link Control protocol. It is the property protocol of Cisco which is the default encapsulation operated with Cisco routers. ]
li => [ HDLC adds the information in a data frame that allows the devices to control the data flow. ]
li => [ HDLC is a bit-oriented protocol that supports both half and full duplex communication. ]
li => [ HDLC offers flexibility, adaptability, reliability, and efficiency of operation for synchronous data communication. ]
li => [ It supports both point-to-point and point-to-multipoint communication. ]
li => [ It supports synchronous as well as asynchronous communication. ]
li => [ It provides full data transparency, i.e., the output delivered has the same bit sequence as the input without any restriction. ]
h3 => [ 8) What are the advantages of LAN switching? ]
p => [  LAN switching : LAN switching enables the multiple users to communicate with each other directly. LAN switching provides the collision-free network and high-speed networking. ]
strong => [ Following are the main advantages of LAN switching: ]
strong => [ Increased network scalability ]
strong => [ Improved bandwidth performance ]
strong => [ Multiple simultaneous connections ]
strong => [ Reduced congestion and transmission delay ]
strong => [ No single point of failure ]
strong => [ Allows full duplex data transmission ]
h3 => [ 9) What is DLCI? ]
p => [  DLCI  stands for  Data Link Connection Identifiers . These are normally assigned by a frame relay service provider to identify each virtual circuit that exists on the network uniquely. ]
h3 => [ 10) What are the different types of networks? ]
p => [ These are the two major types of networks: ]
strong => [ 1. Peer-to-Peer Network: ]
li => [ In a peer-to-peer network, 'peers' are the computers which are connected to each other through an internet connection. ]
li => [ The computer systems on the network without the need for any computer server. ]
li => [ Therefore, the computer in P2P is a "computer server" as well as a "client". ]
li => [ Requirements for a computer to have a peer-to-peer network are the internet connection and P2P software. ]
li => [ Some of the common P2P software peers include Kazaa, Limewire, BearShare, Morpheus, and Acquisition. ]
li => [ Once we are connected to the P2P network, then we able to search the files on other people's computer. ]
strong => [ Types of a peer-to-peer network: ]
strong => [ Pure P2P ]
strong => [ Hybrid P2P ]
strong => [ Mixed P2P ]
strong => [ 2. Server-based Network ]
li => [ In a server-based network, server act as a base for the network known as a central server. ]
li => [ The central server handles multiple tasks such as authenticating users, storing files, managing printers, and running applications such as database and email programs. ]
li => [ In case of a server-based network, security is centralized in the system which allows the user to have one login id and password to log on to any computer system. ]
li => [ Server-based networks are more complex and costly and often requires full- time services for administration. ]
li => [ In server-based networks, the majority of traffic occurs between the servers. ]
h3 => [ 11) What is the difference between private IP and public IP? ]
strong => [ Following are the differences between public IP address and private IP address: ]
th => [ Basis of Comparison ]
th => [ Public IP address ]
th => [ Private IP address ]
td => [ Definition ]
td => [ It is used for the identification of a home network to the outside world. ]
td => [ It is used for the identification of a network device within the home network. ]
td => [ Uniqueness ]
td => [ Public IP address is unique throughout the network. ]
td => [ Private IP address can be the same of two different networks assigned to different computers. ]
td => [ Example ]
td => [ 202.60.23.1 ]
td => [ 192.168.0.3 ]
td => [ Usage ]
td => [ It is used over the internet or other WAN. ]
td => [ This type of address can be used on a local area network or for the computers that are not connected to the internet. ]
td => [ Communication ]
td => [ Public IP address is routable. Therefore, communication among different users is possible. ]
td => [ Private IP address is not routable. Thus, communication among different users is not possible. ]
h3 => [ 12) What is the difference among straight cable, cross cable and rollover cable? ]
strong => [ Straight cable: ]
li => [ Straight cable is used to connect different group devices. For example Switch- Router. ]
li => [ Straight cable is a kind of twisted pair cable used in a local area network to connect a computer to a network hub such as a router. ]
li => [ Straight cables are used for linking different devices. ]
li => [ It is an 8 wired patch cable. ]
li => [ It is also used for connecting PC to the switch or router to a hub. ]
li => [ The main purpose of a straight cable is to connect a host to the client. ]
strong => [ Cross cable: ]
li => [ Cross cable is used to connect the same group devices. For example Switch-Switch. ]
li => [ Cross cable is a cable used to interconnect two computers by reversing their respective pin contacts. ]
li => [ Cross cable is a cross-wired cable used to connect the two computers or hosts directly. ]
li => [ Cross cable is used when two similar devices are to be connected. ]
li => [ Cross cable crisscross each other, and this makes the communication of two devices at the same time. ]
strong => [ Rollover cable: ]
li => [ Rollover cable is used to connect the console port to the computer. ]
li => [ Rollover cable is used to connect the computer's terminal to the network's router console port. ]
li => [ Rollover cable is referred to as a Cisco console cable, and it is flat and light blue in color. ]
li => [ Another name of a rollover cable is Yost cable. ]
li => [ Rollover cable is identified by comparing the end of the cable with another cable as rollover cables are beside each other. ]
li => [ Rollover cable allows the programmer to connect to the network device and can manipulate the programming whenever required. ]
h3 => [ 13) What is the difference between tracert and traceroute? ]
p => [ Differences between tracert and traceroute ]
th => [ Basis of Comparison ]
th => [ tracert ]
th => [ traceroute ]
td => [ Description ]
td => [ The tracert command is a command prompt command used to show the route that the packet takes to move from the source to the destination whatever we specify. ]
td => [ The traceroute command is a command used to show the route from your computer to the destination that you specify. ]
td => [ Device used ]
td => [ The tracert command is used on pc. ]
td => [ The traceroute command is used on a router or switch. ]
td => [ Operating system ]
td => [ The tracert command is used in Windows NT based OS. ]
td => [ The traceroute command is used in UNIX OS. ]
h3 => [ 14) Explain the terms Unicast, Multicast, Broadcast. ]
strong => [ Unicast: ]
li => [ It specifies one to one communication. ]
li => [ It is a communication technique in which data communication takes place between two devices present in the network. ]
li => [ Consider an example of browsing the internet. When we sent a request for some page to the web server, then the request directly goes to the web server to locate the address of a requested page. Therefore, this is one to one communication between client and server. ]
li => [ Downloading the files from the FTP server is also the best example of unicast communication. ]
strong => [ Multicast: ]
li => [ It specifies one to group communication. ]
li => [ It is a communication technique in which data communication takes place between a group of devices. ]
li => [ Multicast uses IGMP(Internet Group Management Protocol) protocol to identify the group. ]
li => [ Consider an example of video conferencing. If any user in a particular group can initiate the call and the people belongs to this group can participate in this call. ]
li => [ Sending e-mail to a particular mailing group can also be considered as the example of multicast communication. ]
strong => [ Broadcast: ]
li => [ It specifies one to all communication. ]
li => [ It is a communication technique in which data communication takes place among all the devices available in the network. ]
li => [ By using a high-level standard, i.e., Message passing interface. It is an interface used for exchanging the messages between multiple computers. ]
li => [ By using a low-level standard, i.e., broadcasting through an ethernet. ]
li => [ Network is not secure in broadcasting as it can lead to a data loss if intruders attack the network. ]
h3 => [ 15) What is the difference between cross cable and straight cable? ]
p => [ Cross cables are used to connect the same group devices while straight cables are used to connect different group devices. ]
p => [  For example : If you want to connect one PC to another PC, you have to use cross cable while, to connect one switch to a router, you have to use a straight cable. ]
h3 => [ 16) What is the difference between static IP addressing and dynamic IP addressing? ]
strong => [ Following are the differences between static IP addressing and dynamic IP addressing: ]
th => [ Basis of Comparison ]
th => [ Static IP address ]
th => [ Dynamic IP address ]
td => [ Description ]
td => [ Static IP address is a fixed number assigned to the computer. ]
td => [ The dynamic IP address is a temporary number assigned to the computer. ]
td => [ Provided By ]
td => [ Static IP address is provided by ISP(Internet Service Provider). ]
td => [ The dynamic IP address is provided by DHCP(Dynamic Host Configuration Protocol). ]
td => [ Change requirement ]
td => [ It is static means that IP address does not change. ]
td => [ It is non-static means that IP address changes whenever the user connects to a network. ]
td => [ Security ]
td => [ It is not secure as IP address is constant. ]
td => [ It is secure because each time IP address changes. ]
td => [ Cost ]
td => [ It is costlier than Dynamic IP address. ]
td => [ It is cheaper than the Static IP address. ]
td => [ Device tracking ]
td => [ Static IP address is trackable as IP address is constant. ]
td => [ The dynamic IP address is untraceable as IP address is always changing. ]
h3 => [ 17) What is the difference between CSMA/CD and CSMA/CA? ]
p => [  CSMA/CD  stands for  Carrier Sense Multiple Access with Collision Detection . It is a media access control method used in local area networking using early Ethernet technology to overcome the occurred collision. ]
p => [  CSMA/CA  stands for Carrier Sense Multiple Access with Collision Avoidance. It is used in the wireless network to avoid the collision. ]
strong => [ Following are the differences between CSMA/CD and CSMA/CA: ]
th => [ CSMA/CD ]
th => [ CSMA/CA ]
td => [ Full form of CSMA/CD is carrier sense multiple access with collision detection. ]
td => [ Full form of CSMA/CA is carrier sense multiple access with carrier avoidance. ]
td => [ CSMA/CD detects the collision, and once the collision is detected, then it stops continuing the data transmission. ]
td => [ CSMA/CA does not deal with the recovery of the collision. ]
td => [ Wired installation is used in a CSMA/CD to detect the collision. ]
td => [ Wireless installation is used in a CSMA/CA as it avoids the collision. Therefore, it does not need a wired network. ]
td => [ An 802.3 Ethernet network uses CSMA/CD. ]
td => [ An 802.11 ethernet network uses CSMA/CA. ]
td => [ CSMA/CD takes effect after the occurrence of a collision. ]
td => [ CSMA/CA takes effect before the occurrence of a collision. ]
h3 => [ 18) What is the purpose of Data Link Layer? ]
p => [ The main purpose of the data link layer is to check that whether messages are sent to the right devices. Another function of the data link layer is framing. ]
h3 => [ 19) What is VLAN? ]
p => [ VLAN stands for Virtual Local Area Network. ]
h3 => [ 20) What is the subnet? Why is it used? ]
p => [ Subnets are used in IP network to break up the larger network into the smaller network. It is used to optimize the performance of the network because it reduces traffic by breaking the larger network into smaller networks. It is also used to identify and isolate network's problem and simplify them. ]
h3 => [ 21) What is the difference between communication and transmission? ]
p => [ Communication is a process of sending and receiving data from an externally connected data cable whereas transmission is a process of transmitting data from source to destination. ]
h3 => [ 22) What is Topology in CCNA? ]
p => [ Topology is an arrangement of various elements (links, nodes, etc.) of a computer network in a specific order. These are the different types of topology used in CCNA: ]
strong => [ Bus: ]
li => [ Bus topology is a network topology in which all the nodes are connected to a single cable known as a central cable or bus. ]
li => [ It acts as a shared communication medium, i.e., if any device wants to send the data to other devices, then it will send the data over the bus which in turn sends the data to all the attached devices. ]
li => [ Bus topology is useful for a small number of devices. As if the bus is damaged then the whole network fails. ]
strong => [ Star: ]
li => [ Star topology is a network topology in which all the nodes are connected to a single device known as a central device. ]
li => [ Star topology requires more cable compared to other topologies. Therefore, it is more robust as a failure in one cable will only disconnect a specific computer connected to this cable. ]
li => [ If the central device is damaged, then the whole network fails. ]
li => [ Star topology is very easy to install, manage and troubleshoot. ]
li => [ Star topology is commonly used in office and home networks. ]
strong => [ Ring: ]
li => [ Ring topology is a network topology in which nodes are exactly connected to two or more nodes and thus, forming a single continuous path for the transmission. ]
li => [ It does not need any central server to control the connectivity among the nodes. ]
li => [ If the single node is damaged, then the whole network fails. ]
li => [ Ring topology is very rarely used as it is expensive, difficult to install and manage. ]
li => [ Examples of Ring topology are SONET network, SDH network, etc. ]
strong => [ Mesh: ]
li => [ Mesh topology is a network topology in which all the nodes are individually connected to other nodes. ]
li => [ It does not need any central switch or hub to control the connectivity among the nodes. ]
strong => [ Fully connected mesh topology ]
strong => [ Partially connected mesh topology ]
li => [ It is a robust as a failure in one cable will only disconnect the specified computer connected to this cable. ]
li => [ Mesh topology is rarely used as installation and configuration are difficult when connectivity gets more. ]
li => [ Cabling cost is high as it requires bulk wiring. ]
strong => [ Tree: ]
li => [ Tree topology is a combination of star and bus topology. It is also known as the expanded star topology. ]
li => [ In tree topology, all the star networks are connected to a single bus. ]
li => [ Ethernet protocol is used in this topology. ]
li => [ In this, the whole network is divided into segments known as star networks which can be easily maintained. If one segment is damaged, but there is no effect on other segments. ]
li => [ Tree topology depends on the "main bus," and if it breaks, then the whole network gets damaged. ]
strong => [ Hybrid: ]
li => [ A hybrid topology is a combination of different topologies to form a resulting topology. ]
li => [ If star topology is connected with another star topology, then it remains star topology. If star topology is connected with different topology, then it becomes a Hybrid topology. ]
li => [ It provides flexibility as it can be implemented in a different network environment. ]
li => [ The weakness of a topology is ignored, and only strength will be taken into consideration. ]
h3 => [ 23) What is the passive topology in CCNA? ]
p => [ When the topology enables the computers on the network only to listen and receive the signals, it is known as passive topology because they don't amplify the signals anyway. ]
h3 => [ 24) What is RAID in CCNA? ]
p => [ RAID stands for Redundant Array of Independent Disks. RAID is a method which is used to standardize and categorize fault-tolerant disk systems. RAID levels provide various facilities like performance, cost, reliability, etc. These three are the mostly used RAID levels: ]
li => [ Level 0: (Striping) ]
li => [ Level 1: (Mirroring) ]
li => [ Level 5: (Striping and Parity) ]
h3 => [ 25) What is the point-to-point protocol in CCNA? ]
p => [ The point-to-point protocol is an industry standard suite of protocols which uses the point-to-point link to transport multiprotocol datagram. The point-to-point protocol is a WAN protocol used at layer 2 to encapsulate the frames for the data transmission over the physical layer. ]
strong => [ Following are the features that point-to-point protocol provides: ]
strong => [ Link quality management ]
li => [ The point-to-point protocol also provides authentication. ]
li => [ It provides some essential features such as authentication, error detection, link quality monitoring, load balancing, compression, etc. ]
strong => [ Components of a point-to-point protocol are: ]
strong => [ Encapsulation ]
strong => [ Link Control Protocol ]
strong => [ Network Control Protocol ]
h3 => [ 26) What are the possible ways of data transmission in CCNA? ]
p => [ Simplex, half-duplex and full-duplex are the communication channels used to convey the information. Either the communication channel can be a physical medium or logical medium. ]
p => [ These are the three possible ways of data transmission: ]
strong => [ Simplex ]
li => [ The simplex communication channel sends the data only in one direction. ]
li => [ Example of the simplex communication channel is a radio station. The radio station transmits the signal while the other receives the signal. ]
li => [ In simplex mode, entire bandwidth can be utilized for the data transmission as a flow of data is in one direction. ]
strong => [ Half-duplex ]
li => [ The half-duplex communication channel sends the information in both the directions but not at the same time. ]
li => [ Performance of half-duplex is better than the simplex communication channel as the data flows in both the directions. ]
li => [ Example of the half-duplex communication channel is "walkie-talkie". In "walkie-talkie", both the transmitter and receiver can communicate with each other on the same channel. ]
li => [ In half-duplex mode, entire bandwidth can be used by the transmitter when the message is sent over the communication channel. ]
strong => [ Full-duplex ]
li => [ The full-duplex communication channel can send the information in both the directions at the same time. ]
li => [ Performance of full-duplex is better than the half-duplex communication channel as the data flows in both the direction at the same time. ]
li => [ Example of a full-duplex communication channel is "telephone". In the case of telephone, one can speak and hear at the same time. Therefore, this channel increases the efficiency of communication. ]
h3 => [ 27) What are the protocol data units (PDU) in CCNA? ]
p => [ Protocol data units (PDU) are the minimum possible units used at different layers of the OSI model to transport data. ]
th => [ Layers ]
th => [ PDU ]
td => [ Transport ]
td => [ Segments ]
td => [ Network ]
td => [ Packets/Datagrams ]
td => [ Data-link ]
td => [ Frames ]
td => [ Physical ]
td => [ Bits ]
h3 => [ 28) What is the difference between RIP and IGRP? ]
strong => [ Following are the differences between RIP and IGRP: ]
th => [ Basis of Comparison ]
th => [ RIP ]
th => [ IGRP ]
td => [ Full form ]
td => [ RIP stands for routing information protocol. ]
td => [ IGRP stands for interior gateway routing protocol. ]
td => [ Description ]
td => [ RIP is a distance vector-based routing protocol. ]
td => [ IGRP is a distance vector based interior gateway routing protocol. ]
td => [ Determination of route ]
td => [ RIP depends on the number of hops to determine the best route to the network. ]
td => [ IGRP considers many factors before decides the best route to take, i.e., bandwidth, reliability, MTU and hops count. ]
td => [ Standard ]
td => [ RIP is a industry standard dynamic protocol. ]
td => [ IGRP is a Cisco standard dynamic protocol. ]
td => [ Organization used ]
td => [ RIP is mainly used for smaller sized organizations. ]
td => [ IGRP is mainly used for medium to large-sized organizations. ]
td => [ Maximum routers ]
td => [ It supports maximum 15 routers. ]
td => [ It supports a maximum 255 routers. ]
td => [ Symbol used ]
td => [ RIP is denoted by 'R' in the routing table. ]
td => [ IGRP is denoted by 'I' in the routing table. ]
td => [ Administrative distance ]
td => [ The administrative distance of RIP is 120. ]
td => [ The administrative distance of IGRP is 100. ]
td => [ Algorithm ]
td => [ RIP works on Bellman ford Algorithm. ]
td => [ IGRP works on Bellman ford Algorithm. ]
h3 => [ 29) What are the different memories used in a CISCO router? ]
p => [ Three types of memories are used in a CISCO router: ]
strong => [ NVRAM ]
li => [ NVRAM stands for Non-volatile random access memory. ]
li => [ It is used to store the startup configuration file. ]
li => [ NVRAM retains the configuration file even if the router shut down. ]
strong => [ DRAM ]
li => [ DRAM stands for dynamic random access memory. ]
li => [ It stores the configuration file that is being executed. ]
li => [ DRAM is used by the processor to access the data directly rather than accessing it from scratch. ]
li => [ DRAM is located near the processor that provides the faster access to the data than the storage media such as hard disk. ]
li => [ Simple design, low cost, and high speed are the main features of DRAM memory. ]
li => [ DRAM is a volatile memory. ]
strong => [ Flash Memory ]
li => [ It is used to store the system IOS. ]
li => [ Flash memory is used to store the ios images. ]
li => [ Flash memory is erasable and reprogrammable ROM. ]
li => [ The capacity of the flash memory is large enough to accommodate many different IOS versions. ]
h3 => [ 30) What is the difference between full-duplex and half-duplex? ]
strong => [ Following are the differences between half-duplex and full-duplex ]
th => [ Basis of Comparison ]
th => [ Half-duplex ]
th => [ Full-duplex ]
td => [ Direction of communication ]
td => [ Communication is bi-directional but not at the same time. ]
td => [ Communication is bi-directional and done at the same time. ]
td => [ Send/receive ]
td => [ A sender can send as well as receive the data but not at the same time.	 ]
td => [ A sender can send as well as receive the data simultaneously. ]
td => [ Performance ]
td => [ Performance of half-duplex mode is not as good as a full-duplex mode. ]
td => [ Performance of full-duplex mode is better than the half-duplex mode. ]
td => [ Example ]
td => [ Example of half-duplex is a walkie-talkie. ]
td => [ Example of full-duplex is a telephone. ]
h3 => [ 31) What is BootP? ]
p => [ BootP is a short form of Boot Program. It is a protocol that is used to boot diskless workstation connected to the network. BootP is also used by diskless workstations to determine its IP address and also the IP addresses of server PC. ]
h3 => [ 32) What is a Frame Relay? ]
p => [ Frame Relay is used to provide connection-oriented communication by creating and maintaining virtual circuits. It is a WAN protocol that is operated at the Data Link and physical layer to sustain high-performance rating. ]
strong => [ How frame relay works. ]
p => [ Frame relay multiplexes the traffic coming from different connections over a shared physical medium using special purpose hardware components such as routers, bridges, switch that packages the data into a frame relay messages. It reduces the network latency, i.e., the number of delays. It also supports the variable sized packet for the efficient utilization of network bandwidth. ]
h3 => [ 33) What is Latency? ]
p => [ Latency is the amount of time delay. It is measured as the time difference between at the point of time when a network receives the data, and the time it is sent by another network. ]
h3 => [ 34) What is the MAC address? ]
p => [ MAC address stands for Media Access Control address. This is an address of a device which is identified as the Media Access Control Layer in the network architecture. The MAC address is unique and usually stored in ROM. ]
h3 => [ 35) What is the difference between ARP and RARP? ]
p => [  ARP  stands for Address Resolution Protocol. ARP is a protocol that is used to map an IP address to a physical machine address. ]
p => [  RAPR  stands for Reverse Address Resolution Protocol. RARP is a protocol that is used to map a MAC address to IP address. ]
p => [ Following are the differences between ARP and RARP: ]
th => [ Basis of Comparison ]
th => [ ARP ]
th => [ RARP ]
td => [ Full form ]
td => [ Full form of ARP is address resolution protocol. ]
td => [ Full form of RARP is reverse address resolution protocol. ]
td => [ Description ]
td => [ ARP contains the logical address, and it retrieves the physical address of the receiver. ]
td => [ RARP includes the physical address and retrieves the logical address of a computer from the server. ]
td => [ Mapping ]
td => [ ARP is used to map 32-bit logical address to 48-bit physical address. ]
td => [ RARP is used to map 48-bit physical address to 32-bit logical address. ]
h3 => [ 36) What is the size of an IP address? ]
p => [ The size for IPv4 is 32 bits and 128 bits for IPv6. ]
h3 => [ 37) What is Ping? What is the usage of Ping? ]
p => [ PING stands for Packet Internet Groper. It is a computer network tool which is used to test whether a particular host is reachable across an IP address or not. ]
h3 => [ 38) What is the checksum? ]
p => [ The checksum is a simple error detection scheme in which each transmitted message is accompanied by a numerical value based on the number of set bits in the message. ]
h3 => [ 39) What are the different types of the password used in securing a Cisco router? ]
p => [ There are five types of passwords can be set on a Cisco router: ]
li => [ Consol ]
li => [ Aux ]
li => [ VTY ]
li => [ Enable Password ]
li => [ Enable Secret ]
h3 => [ 40) What is the usage of Service Password Encryption? ]
p => [ Service Password Encryption command is used to encrypt all passwords on your router to hide from your running config. ]
a => [ Java Basics Interview Questions ]
a => [ Java OOPs Interview Questions ]
a => [ Java Multithreading Interview Questions ]
a => [ Java String &amp; Exception Interview Questions ]
a => [ Java Collection Interview Questions ]
a => [ JDBC Interview Questions ]
a => [ Servlet Interview Questions ]
a => [ JSP Interview Questions ]
span => [ Spring Interview Questions ]
span => [ Hibernate Interview Questions ]
span => [ PL/SQL Interview Questions ]
span => [ SQL Interview Questions ]
span => [ Oracle Interview Questions ]
span => [ Android Interview Questions ]
span => [ SQL Server Interview Questions ]
span => [ MySQL Interview Questions ]
h2 => [ You may also like: ]
a => [ Java Interview Questions ]
a => [ SQL Interview Questions ]
a => [ Python Interview Questions ]
a => [ JavaScript Interview Questions ]
a => [ Angular Interview Questions ]
a => [ Selenium Interview Questions ]
a => [ Spring Boot Interview Questions ]
a => [ HR Interview Questions ]
a => [ C Programming Interview Questions ]
a => [ C++ Interview Questions ]
a => [ Data Structure Interview Questions ]
a => [ DBMS Interview Questions ]
a => [ HTML Interview Questions ]
a => [ IAS Interview Questions ]
a => [ Manual Testing Interview Questions ]
a => [ OOPs Interview Questions ]
a => [ .Net Interview Questions ]
a => [ C# Interview Questions ]
a => [ ReactJS Interview Questions ]
a => [ Networking Interview Questions ]
a => [ PHP Interview Questions ]
a => [ CSS Interview Questions ]
a => [ Node.js Interview Questions ]
a => [ Spring Interview Questions ]
a => [ Hibernate Interview Questions ]
a => [ AWS Interview Questions ]
a => [ Accounting Interview Questions ]
h2 => [ Learn Latest Tutorials ]
p => [ Splunk ]
p => [ SPSS ]
p => [ Swagger ]
p => [ Transact-SQL ]
p => [ Tumblr ]
p => [ ReactJS ]
p => [ Regex ]
p => [ Reinforcement Learning ]
p => [ R Programming ]
p => [ RxJS ]
p => [ React Native ]
p => [ Python Design Patterns ]
p => [ Python Pillow ]
p => [ Python Turtle ]
p => [ Keras ]
h2 => [ Preparation ]
p => [ Aptitude ]
p => [ Reasoning ]
p => [ Verbal Ability ]
p => [ Interview Questions ]
p => [ Company Questions ]
h2 => [ Trending Technologies ]
p => [ Artificial Intelligence ]
p => [ AWS ]
p => [ Selenium ]
p => [ Cloud Computing ]
p => [ Hadoop ]
p => [ ReactJS ]
p => [ Data Science ]
p => [ Angular 7 ]
p => [ Blockchain ]
p => [ Git ]
p => [ Machine Learning ]
p => [ DevOps ]
h2 => [ B.Tech / MCA ]
p => [ DBMS ]
p => [ Data Structures ]
p => [ DAA ]
p => [ Operating System ]
p => [ Computer Network ]
p => [ Compiler Design ]
p => [ Computer Organization ]
p => [ Discrete Mathematics ]
p => [ Ethical Hacking ]
p => [ Computer Graphics ]
p => [ Software Engineering ]
p => [ Web Technology ]
p => [ Cyber Security ]
p => [ Automata ]
p => [ C Programming ]
p => [ C++ ]
p => [ Java ]
p => [ .Net ]
p => [ Python ]
p => [ Programs ]
p => [ Control System ]
p => [ Data Mining ]
p => [ Data Warehouse ]
