
div => [ 
 ]
h1 => [ CSS Interview Questions ]
p => [  A list of top frequently asked  CSS interview questions  and answers are given below.  ]
h3 => [ 1) What is CSS? ]
p => [  CSS stands for Cascading Style Sheet. It is a popular styling language which is used with HTML to design websites. It can also be used with any XML documents including plain XML, SVG, and XUL. More details... ]
h3 => [ 2) What is the origin of CSS? ]
p => [ SGML (Standard Generalized Markup Language) is the origin of CSS. It is a language that defines markup languages.  ]
h3 => [ 3) What are the different variations of CSS? ]
p => [ Following are the different variations of CSS: ]
li => [ CSS1 ]
li => [ CSS2 ]
li => [ CSS2.1 ]
li => [ CSS3 ]
li => [ CSS4 ]
h3 => [ 4) How can you integrate CSS on a web page? ]
p => [ There are three methods to integrate CSS on web pages. ]
li => [ Inline method - It is used to insert style sheets in HTML document ]
li => [ Embedded/Internal method - It is used to add a unique style to a single document ]
li => [ Linked/Imported/External method - It is used when you want to make changes on multiple pages. ]
a => [ More details... ]
h3 => [ 5) What are the advantages of CSS? ]
li => [ Bandwidth ]
li => [ Site-wide consistency ]
li => [ Page reformatting ]
li => [ Accessibility ]
li => [ Content separated from presentation ]
h3 => [ 6) What are the limitations of CSS? ]
li => [ Ascending by selectors is not possible ]
li => [ Limitations of vertical control ]
li => [ No expressions ]
li => [ No column declaration ]
li => [ Pseudo-class not controlled by dynamic behavior ]
li => [ Rules, styles, targeting specific text not possible ]
h3 => [ 7) What are the CSS frameworks? ]
p => [ CSS frameworks are the preplanned libraries which make easy and more standard compliant web page styling. The frequently used CSS frameworks are: - ]
li => [ Bootstrap ]
li => [ Foundation ]
li => [ Semantic UI ]
li => [ Gumby ]
li => [ Ulkit ]
h3 => [ 8) Why background and color are the separate properties if they should always be set together? ]
p => [ There are two reasons behind this: ]
li => [ It enhances the legibility of style sheets. The background property is a complex property in CSS, and if it is combined with color, the complexity will further increase.  ]
li => [ Color is an inherited property while the background is not. So this can make confusion further. ]
h3 => [ 9) What is Embedded Style Sheet? ]
p => [  An Embedded style sheet is a CSS style specification method used with HTML. You can embed the entire stylesheet in an HTML document by using the STYLE element.  More details... ]
h3 => [ 10) What are the advantages of Embedded Style Sheets? ]
li => [ You can create classes for use on multiple tag types in the document. ]
li => [ You can use selector and grouping methods to apply styles in complex situations. ]
li => [ No extra download is required to import the information. ]
h3 => [ 11) What is a CSS selector? ]
p => [ It is a string that identifies the elements to which a particular declaration apply. It is also referred as a link between the HTML document and the style sheet. It is equivalent of HTML elements. There are several different types of selectors in CSS: -  ]
li => [ CSS Element Selector ]
li => [ CSS Id Selector ]
li => [ CSS Class Selector ]
li => [ CSS Universal Selector ]
li => [ CSS Group Selector ]
a => [ More details... ]
h3 => [ 12) Name some CSS style components. ]
p => [ Some CSS Style components are: ]
li => [ Selector ]
li => [ Property ]
li => [ Value ]
h3 => [ 13) What is the use of CSS Opacity? ]
p => [ The CSS opacity property is used to specify the transparency of an element. In simple word, you can say that it specifies the clarity of the image.
In technical terms, Opacity is defined as the degree to which light is allowed to travel through an object. For example: ]
h3 => [ 14) Explain universal selector. ]
p => [ The universal selector matches the name of any of the element type instead of selecting elements of a specific type. ]
h3 => [ 15) Which command is used for the selection of all the elements of a paragraph? ]
p => [ The p[lang] command is used for selecting all the elements of a paragraph. ]
h3 => [ 16) What is the use of % unit?  ]
p => [ It is used for defining percentage values. ]
h3 => [ 17) Name the property used to specify the background color of an element. ]
p => [ The background-color property is used to specify the background color of the element. For example:  ]
h3 => [ 18) Name the property for controlling the image repetition of the background. ]
p => [ The background-repeat property repeats the background image horizontally and vertically. Some images are repeated only horizontally or vertically.  ]
h3 => [ 19) Name the property for controlling the image position in the background. ]
p => [  The background-position property is used to define the initial position of the background image. By default, the background image is placed on the top-left of the webpage.  ]
p => [ You can set the following positions: ]
li => [ center ]
li => [ top ]
li => [ bottom ]
li => [ left ]
li => [ right ]
h3 => [ 20) Name the property for controlling the image scroll in the background.  ]
p => [ The background-attachment property is used to specify if the background image is fixed or scroll with the rest of the page in the browser window. If you set fixed the background image, then the image not move during scrolling in the browser. Let's take an example with the fixed background image.  ]
h3 => [ 21) What is the use of ruleset? ]
p => [ The ruleset is used to identify that selectors can be attached with other selectors. It has two parts: ]
li => [ Selector - Selector indicates the HTML element you want to style.  ]
li => [ Declaration Block - The declaration block can contain one or more declarations separated by a semicolon. ]
h3 => [ 22) What is the difference between class selectors and id selectors? ]
p => [ An overall block is given to class selector while id selectors take only a single element differing from other elements. ]
h4 => [ CSS Class Selector ]
h4 => [ CSS Id Selector ]
p => [    More details... ]
h3 => [ 23) What are the advantages of External Style Sheets? ]
li => [ You can create classes for reusing it in many documents. ]
li => [ By using it, you can control the styles of multiple documents from one file.  ]
li => [ In complex situations, you can use selectors and grouping methods to apply styles.  ]
a => [ More details... ]
h3 => [ 24) What is the difference between inline, embedded and external style sheets? ]
p => [  Inline : Inline Style Sheet is used to style only a small piece of code.  ]
h4 => [ Syntax ]
p => [  Embedded : Embedded style sheets are put between the &lt;head&gt;...&lt;/head&gt; tags. ]
h4 => [ Syntax ]
p => [  External : This is used to apply the style to all the pages within your website by changing just one style sheet.  ]
h4 => [ Syntax ]
h3 => [ 25) What is RWD? ]
p => [ RWD stands for Responsive Web Design. This technique is used to display the designed page perfectly on every screen size and device, for example, mobile, tablet, desktop and laptop. You don't need to create a different page for each device.  ]
h3 => [ 26) What are the benefits of CSS sprites? ]
p => [  If a web page has a large number of images that take a longer time to load because each image separately sends out an HTTP request. The concept of CSS sprites is used to reduce the loading time for a web page because it combines the various small images into one image. It reduces the number of HTTP requests and hence the loading time.  ]
h3 => [ 27) What is the difference between logical tags and physical tags? ]
li => [ Physical tags are referred to as presentational markup while logical tags are useless for appearances. ]
li => [ Physical tags are newer versions, on the other hand, logical tags are old and concentrate on content.  ]
h3 => [ 28) What is the CSS Box model and what are its elements? ]
p => [ The CSS box model is used to define the design and layout of elements of CSS.  ]
p => [ The elements are: ]
li => [ Margin - It removes the area around the border. It is transparent. ]
li => [ Border - It represents the area around the padding ]
li => [ Padding - It removes the area around the content. It is transparent. ]
li => [ Content - It represents the content like text, images, etc. ]
h3 => [ 29) What is the float property of CSS? ]
p => [ The CSS float property is used to move the image to the right or left along with the texts to be wrapped around it. It doesn't change the property of the elements used before it. ]
p => [ To understand its purpose and origin, let's take a look at its print display. In the print display, an image is set into the page such that text wraps around it as needed.  ]
p => [ Its web layout is also just similar to print layout. ]
a => [ More details... ]
h3 => [ 30) How to restore the default property value using CSS? ]
p => [ In short, there is no easy way to restore to default values to whatever a browser uses. ]
p => [ The closest option is to use the 'initial' property value, which restores the default CSS values, rather than the browser's default styles. ]
h3 => [ 31) What is the purpose of the z-index and how is it used? ]
p => [ The z-index helps to specify the stack order of positioned elements that may overlap one another. The z-index default value is zero and can take on either a positive or negative number. ]
p => [ An element with a higher z-index is always stacked above than a lower index. ]
p => [ Z-Index can take the following values: ]
strong => [ Auto: ]
strong => [ Number: ]
strong => [ Initial: ]
strong => [ Inherit: ]
h3 => [ 32) Explain the difference between visibility: hidden and display: none? ]
p => [  visibility: hidden  hides the element, but it occupies space and affects the layout of the document. ]
p => [  display: none  also hides the element but not occupy space. It will not affect the layout of the document. ]
h3 => [ 33) What do you understand by W3C? ]
p => [ W3C stands for World Wide Web Consortium. Its purpose is to deliver the information of the World Wide Web. It also develops rules and guidelines for the Web. ]
h3 => [ 34) What is tweening? ]
p => [ It is the process of generating intermediate frames between two images.  ]
p => [ It gives the impression that the first image has smoothly evolved into the second one.  ]
p => [ It is an important method used in all types of animations.  ]
p => [ In CSS3, Transforms (matrix, translate, rotate, scale) module can be used to achieve tweening. ]
h3 => [ 35) What is the difference between CSS2 and CSS3? ]
p => [ The main difference between CSS2 and CSS3 is that CSS3 is divided into different sections which are also known as modules. Unlike CSS2, CSS3 modules are supported by many browsers. ]
p => [  Apart from that, CSS3 contains new General Sibling Combinators which is responsible for matching the sibling elements with the given elements. ]
a => [ Interview Tips ]
a => [ Job/HR Interview Questions ]
a => [ JavaScript Interview Questions ]
a => [ jQuery Interview Questions ]
a => [ Java Basics Interview Questions ]
a => [ Java OOPs Interview Questions ]
a => [ Servlet Interview Questions ]
a => [ JSP Interview Questions ]
span => [ Spring Interview Questions ]
span => [ Hibernate Interview Questions ]
span => [ PL/SQL Interview Questions ]
span => [ SQL Interview Questions ]
span => [ Oracle Interview Questions ]
span => [ Android Interview Questions ]
span => [ SQL Server Interview Questions ]
span => [ MySQL Interview Questions ]
h2 => [ You may also like: ]
a => [ Java Interview Questions ]
a => [ SQL Interview Questions ]
a => [ Python Interview Questions ]
a => [ JavaScript Interview Questions ]
a => [ Angular Interview Questions ]
a => [ Selenium Interview Questions ]
a => [ Spring Boot Interview Questions ]
a => [ HR Interview Questions ]
a => [ C Programming Interview Questions ]
a => [ C++ Interview Questions ]
a => [ Data Structure Interview Questions ]
a => [ DBMS Interview Questions ]
a => [ HTML Interview Questions ]
a => [ IAS Interview Questions ]
a => [ Manual Testing Interview Questions ]
a => [ OOPs Interview Questions ]
a => [ .Net Interview Questions ]
a => [ C# Interview Questions ]
a => [ ReactJS Interview Questions ]
a => [ Networking Interview Questions ]
a => [ PHP Interview Questions ]
a => [ CSS Interview Questions ]
a => [ Node.js Interview Questions ]
a => [ Spring Interview Questions ]
a => [ Hibernate Interview Questions ]
a => [ AWS Interview Questions ]
a => [ Accounting Interview Questions ]
h2 => [ Learn Latest Tutorials ]
p => [ Splunk ]
p => [ SPSS ]
p => [ Swagger ]
p => [ Transact-SQL ]
p => [ Tumblr ]
p => [ ReactJS ]
p => [ Regex ]
p => [ Reinforcement Learning ]
p => [ R Programming ]
p => [ RxJS ]
p => [ React Native ]
p => [ Python Design Patterns ]
p => [ Python Pillow ]
p => [ Python Turtle ]
p => [ Keras ]
h2 => [ Preparation ]
p => [ Aptitude ]
p => [ Reasoning ]
p => [ Verbal Ability ]
p => [ Interview Questions ]
p => [ Company Questions ]
h2 => [ Trending Technologies ]
p => [ Artificial Intelligence ]
p => [ AWS ]
p => [ Selenium ]
p => [ Cloud Computing ]
p => [ Hadoop ]
p => [ ReactJS ]
p => [ Data Science ]
p => [ Angular 7 ]
p => [ Blockchain ]
p => [ Git ]
p => [ Machine Learning ]
p => [ DevOps ]
h2 => [ B.Tech / MCA ]
p => [ DBMS ]
p => [ Data Structures ]
p => [ DAA ]
p => [ Operating System ]
p => [ Computer Network ]
p => [ Compiler Design ]
p => [ Computer Organization ]
p => [ Discrete Mathematics ]
p => [ Ethical Hacking ]
p => [ Computer Graphics ]
p => [ Software Engineering ]
p => [ Web Technology ]
p => [ Cyber Security ]
p => [ Automata ]
p => [ C Programming ]
p => [ C++ ]
p => [ Java ]
p => [ .Net ]
p => [ Python ]
p => [ Programs ]
p => [ Control System ]
p => [ Data Mining ]
p => [ Data Warehouse ]
