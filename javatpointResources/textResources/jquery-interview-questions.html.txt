
div => [ 
 ]
h1 => [ jQuery Interview Question ]
p => [  A list of top frequently asked  jQuery interview questions  and answers are given below.  ]
h3 => [ 1) What is jQuery? ]
p => [ jQuery is a fast, lightweight, feature-rich client-side JavaScript library. It is cross-platform and supports different types of browsers. It has provided a much-needed boost to JavaScript. Before jQuery, JavaScript codes were lengthy and bigger, even for smaller functions. It makes a website more interactive and attractive. ]
h3 => [ 2) Is jQuery a programming language? ]
p => [ jQuery is not a programming language but a well-written JavaScript code. It is used to traverse documents, event handling, Ajax interaction, and Animation.  ]
h3 => [ 3) What is the difference between JavaScript and jQuery? ]
p => [ The simple difference is that JavaScript is a language while jQuery is a built-in library built for JavaScript. jQuery simplifies the use of JavaScript language.  ]
h3 => [ 4) Is jQuery replacement of JavaScript? ]
p => [ No, jQuery is not the replacement of JavaScript. jQuery is written on the top of JavaScript, and it is a different library. jQuery is a lightweight JavaScript library which is used to interact with JavaScript and HTML.  ]
h3 => [ 5) Why do we use jQuery? ]
li => [ It is very easy to learn and use. ]
li => [ It is used to develop browser compatible web applications. ]
li => [ It improves the performance of an application.  ]
li => [ It is very fast and extensible.  ]
li => [ It facilitates you to write minimal lines of codes for UI related functions. ]
li => [ It provides cross-browser support. ]
h3 => [ 6) What is $() in jQuery library? ]
p => [ The $() function is an alias of jQuery() function. It is used to wrap any object into jQuery object which later facilitates you to call the various method defined jQuery object. You can pass a selector string to $() function, and it returns a jQuery object which contains an array of all matched DOM elements. ]
h4 => [ Syntax: ]
h3 => [ 7) What are the effects methods used in jQuery? ]
p => [ These are some effects methods used in jQuery: ]
li => [ show() - It displays or shows the selected elements. ]
li => [ hide() - It hides the matched or selected elements. ]
li => [ toggle() - It shows or hides the matched elements. In other words, it toggles between the hide() and shows() methods. ]
li => [ fadeIn() - It shows the matched elements by fading it to opaque. In other words, it fades in the selected elements. ]
li => [ fadeOut() - It shows the matched elements by fading it to transparent. In other words, it fades out the selected elements. ]
h3 => [ 8) What is the use of toggle() method in JQuery? ]
p => [ The jQuery toggle() is a particular type of method which is used to toggle between the hide() and show() method. It shows the hidden elements and hides the shown element. ]
h4 => [ Syntax: ]
p => [  speed : It is an optional parameter. It specifies the speed of the delay. Its possible values are slow, fast and milliseconds. ]
p => [  easing : It specifies the easing function to be used for transition. ]
p => [  callback : It is also an optional parameter. It specifies the function to be called after completion of toggle() effect. ]
p => [  display : If true, it displays an element. If false, it hides the element. ]
p => [  For more:  Click here ]
h3 => [ 9) What is the purpose of fadeToggle() method in JQuery? ]
p => [ The jQuery fadeToggle() method is used to toggle between the fadeIn() and fadeOut() methods. If the elements are faded in, it makes them faded out, and if they are faded out, it makes them faded in. ]
h4 => [ Syntax: ]
p => [  speed : It is an optional parameter. It specifies the speed of the delay. Its possible values are slow, fast and milliseconds. ]
p => [  easing : It specifies the easing function to be used for transition. ]
p => [  callback : It is also an optional parameter. It specifies the function to be called after completion of fadeToggle() effect. ]
p => [  For more:  Click here ]
h3 => [ 10) What is the use of delay() method in JQuery? ]
p => [ The jQuery delay() method is used to delay the execution of functions in the queue. It is the best method to make a delay between the queued jQuery effects. The jQUery delay () method sets a timer to delay the execution of the next item in the queue. ]
h4 => [ Syntax: ]
p => [  speed:  It is an optional parameter. It specifies the speed of the delay. Its possible values are slow, fast and milliseconds. ]
p => [  queueName:  It is also an optional parameter. It specifies the name of the queue. Its default value is "fx" the standard queue effect.  ]
p => [  For more:  Click here ]
h3 => [ 11) Is it possible that jQuery HTML work for both HTML and XML document? ]
p => [ No, jQuery HTML only works for HTML document. It doesn't work for XML documents.  ]
h3 => [ 12) What is the use of html() method in JQuery? ]
p => [ The jQuery html() method is used to change the entire content of the selected elements. It replaces the selected element content with new contents. ]
h4 => [ Syntax: ]
p => [  For complete example:  Click here ]
h3 => [ 13) What is the use of css() method in JQuery? ]
p => [ The jQuery CSS() method is used to get (return)or set style properties or values for selected elements. It facilitates you to get one or more style properties. The jQuery CSS() provides two ways: ]
h4 => [ Return a CSS property ]
p => [  It is used to get the value of a specified CSS property. ]
h4 => [ Set a CSS property  ]
p => [ This property is used to set a specific value for all matched element. ]
p => [  For more:  Click here ]
h3 => [ 14) Is jQuery library used for server scripting or client scripting? ]
p => [ It is a library for client-side Scripting.  ]
h3 => [ 15) Is jQuery a W3C standard? ]
p => [ No, jQuery is not a W3C standard. ]
h3 => [ 16) What is the starting point of code execution in jQuery? ]
p => [ $(document).ready() function is the starting point of jQuery code. It is executed when DOM is loaded. ]
h3 => [ 17) What is the basic requirement to start with the jQuery? ]
p => [ You need refer to its library to start with jQuery. You can download the latest version of jQuery from jQuery.com. ]
h3 => [ 18) Can you use any other name in place of $ (dollar sign) in jQuery? ]
p => [ Yes, instead of $ (dollar sign) we can use jQuery as a function name. For example: ]
h3 => [ 19) Can you use multiple document.ready() function on the same page? ]
p => [ Yes. You can use any number of document.ready() function on the same page. For example: ]
h3 => [ 20) What is the difference between find and children methods? ]
p => [ Find method is used to find all levels down the DOM tree while children method is used to find single level down the DOM tree. ]
h3 => [ 21) What is a CDN? ]
p => [ CDN stands for Content Delivery Network or Content Distribution Network. It is a large distributed system of servers deployed in multiple data centers across the internet. It provides the files from servers at a higher bandwidth that leads to faster loading time. These are several companies that provide free public CDNs: ]
li => [ Google ]
li => [ Microsoft ]
li => [ Yahoo  ]
h3 => [ 22) What is the goal of CDN and what are the advantages of using CDN? ]
p => [ The primary goal of the CDN is to provide content to the end-users with high availability and high performance.  ]
strong => [ Advantages of using CDN: ]
li => [ It reduces the load from the server. ]
li => [ It saves bandwidth. jQuery framework is loaded faster from these CDN. ]
li => [ If a user regularly visits a site which is using jQuery framework from any of these CDN, it will be cached.  ]
h3 => [ 23) How can you use a jQuery library in your project? ]
p => [ You can use a jQuery library in the ASP.Net project from downloading the latest jQuery library from jQuery.com and include the references to the jQuery library file in your HTML/PHP/JSP/Aspx page.  ]
h3 => [ 24) What are the selectors in jQuery? How many types of selectors in jQuery? ]
p => [ If you want to work with an element on the web page, first you need to find it. Selectors find the HTML elements in jQuery. There are many types of selectors. Some basic selectors are: ]
strong => [ Name: ]
strong => [ #ID: ]
strong => [ .Class: ]
strong => [ Universal (*): ]
strong => [ Multiple Elements E, F, G: ]
strong => [ Attribute Selector: ]
h3 => [ 25) What is a use of jQuery filter? ]
p => [ : jQuery filter is used to filter the specific values from the object. It filters the result of your original query into specific elements.  ]
h3 => [ 26) What are the different types of selectors in jQuery? ]
p => [ There are three types of selectors in jQuery: ]
li => [ CSS Selector ]
li => [ Custom Selector ]
li => [ XPath Selector ]
h3 => [ 27) What is the difference between the ID selector and class selector in jQuery? ]
p => [ ID selector and class selector are the same as they are in CSS. ID selector uses ID while class selector uses a class to select elements.  ]
p => [ You use an ID selector to select just one element. If you want to select a group of elements, having the same CSS class, use class selector.
 ]
h3 => [ 28) What is the use of serialize() method in JQuery? ]
p => [ The jQuery serialize() method is used to create a text string in standard URL-encoded notation. It serializes the form values so that its serialized values can be used in the URL query string while making an AJAX request. ]
h4 => [ Syntax: ]
p => [  For complete example:  Click here ]
h3 => [ 29) What is the use of val() method in JQuery? ]
p => [ The jQuery val() method is used: ]
li => [ To get the current value of the first element in the set of matched elements. ]
li => [ To set the value of every matched element. ]
h4 => [ Syntax: ]
p => [  For complete example:  Click here ]
h3 => [ 30) How to add and remove CSS classes to an element using jQuery? ]
p => [ You can use addclass() jQuery method to add CSS class to an element and removeclass() jQuery method to remove CSS class from an element. ]
h4 => [  CSS addClass() Example ]
h4 => [  CSS removeClass() Example ]
h3 => [ 31) Can you write a jQuery code to select all links inside the paragraph? ]
p => [ Yes. You can use &lt;a&gt; tag nested inside paragraph &lt;p&gt; tag to select all links. For example: ]
h3 => [ 32) What is the difference between prop and attr? ]
p => [  attr():  It gets the value of an attribute for the first element in the set of matched element.  ]
p => [  prop():  it gets the value of a property for the first element in the set of matched elements. It is introduced in jQuery 1.6.  ]
h3 => [ 33) What are the two types of CDNs? ]
p => [ There are two types of CDN: ]
strong => [ Microsoft: ]
strong => [ Google: ]
h3 => [ 34) What is the use of the animate() method in jQuery? ]
p => [ The animate function is used to apply the custom animation effect to elements. Syntax: ]
p => [ Here, ]
li => [ "param" defines the CSS properties on which you want to apply the animation. ]
li => [ "duration" specify how long the animation run. It can be one of the following values: "slow," "fast," "normal" or milliseconds ]
li => [ "easing" is the string which specifies the function for the transition. ]
li => [ "callback" is the function which we want to run once the animation effect is complete.  ]
h3 => [ 35) How can you disable jQuery animation? ]
p => [ By using jQuery property "jQuery.fx.off" and setting it to true, you can disable jQuery animation. ]
a => [ Java Basics Interview Questions ]
a => [ Java OOPs Interview Questions ]
a => [ Java Multithreading Interview Questions ]
a => [ Java String &amp; Exception Interview Questions ]
a => [ Java Collection Interview Questions ]
a => [ JDBC Interview Questions ]
a => [ Servlet Interview Questions ]
a => [ JSP Interview Questions ]
span => [ Spring Interview Questions ]
span => [ Hibernate Interview Questions ]
span => [ PL/SQL Interview Questions ]
span => [ SQL Interview Questions ]
span => [ Oracle Interview Questions ]
span => [ Android Interview Questions ]
span => [ SQL Server Interview Questions ]
span => [ MySQL Interview Questions ]
h2 => [ You may also like: ]
a => [ Java Interview Questions ]
a => [ SQL Interview Questions ]
a => [ Python Interview Questions ]
a => [ JavaScript Interview Questions ]
a => [ Angular Interview Questions ]
a => [ Selenium Interview Questions ]
a => [ Spring Boot Interview Questions ]
a => [ HR Interview Questions ]
a => [ C Programming Interview Questions ]
a => [ C++ Interview Questions ]
a => [ Data Structure Interview Questions ]
a => [ DBMS Interview Questions ]
a => [ HTML Interview Questions ]
a => [ IAS Interview Questions ]
a => [ Manual Testing Interview Questions ]
a => [ OOPs Interview Questions ]
a => [ .Net Interview Questions ]
a => [ C# Interview Questions ]
a => [ ReactJS Interview Questions ]
a => [ Networking Interview Questions ]
a => [ PHP Interview Questions ]
a => [ CSS Interview Questions ]
a => [ Node.js Interview Questions ]
a => [ Spring Interview Questions ]
a => [ Hibernate Interview Questions ]
a => [ AWS Interview Questions ]
a => [ Accounting Interview Questions ]
h2 => [ Learn Latest Tutorials ]
p => [ Splunk ]
p => [ SPSS ]
p => [ Swagger ]
p => [ Transact-SQL ]
p => [ Tumblr ]
p => [ ReactJS ]
p => [ Regex ]
p => [ Reinforcement Learning ]
p => [ R Programming ]
p => [ RxJS ]
p => [ React Native ]
p => [ Python Design Patterns ]
p => [ Python Pillow ]
p => [ Python Turtle ]
p => [ Keras ]
h2 => [ Preparation ]
p => [ Aptitude ]
p => [ Reasoning ]
p => [ Verbal Ability ]
p => [ Interview Questions ]
p => [ Company Questions ]
h2 => [ Trending Technologies ]
p => [ Artificial Intelligence ]
p => [ AWS ]
p => [ Selenium ]
p => [ Cloud Computing ]
p => [ Hadoop ]
p => [ ReactJS ]
p => [ Data Science ]
p => [ Angular 7 ]
p => [ Blockchain ]
p => [ Git ]
p => [ Machine Learning ]
p => [ DevOps ]
h2 => [ B.Tech / MCA ]
p => [ DBMS ]
p => [ Data Structures ]
p => [ DAA ]
p => [ Operating System ]
p => [ Computer Network ]
p => [ Compiler Design ]
p => [ Computer Organization ]
p => [ Discrete Mathematics ]
p => [ Ethical Hacking ]
p => [ Computer Graphics ]
p => [ Software Engineering ]
p => [ Web Technology ]
p => [ Cyber Security ]
p => [ Automata ]
p => [ C Programming ]
p => [ C++ ]
p => [ Java ]
p => [ .Net ]
p => [ Python ]
p => [ Programs ]
p => [ Control System ]
p => [ Data Mining ]
p => [ Data Warehouse ]
