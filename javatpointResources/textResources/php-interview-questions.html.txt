
div => [ 
 ]
h1 => [ PHP Interview Questions ]
p => [ There is given PHP interview questions and answers that have been asked in many companies. Let's see the list of top PHP interview questions. ]
h3 => [ 1) What is PHP? ]
p => [  PHP  stands for  Hypertext Preprocessor . It is an open source server-side scripting language which is widely used for web development. It supports many databases like MySQL, Oracle, Sybase, Solid, PostgreSQL, generic ODBC etc. ]
p => [  More Details... 
 ]
h3 => [ 2) What is PEAR in PHP? ]
p => [  PEAR  is a framework and  repository for reusable PHP components . PEAR stands for  PHP Extension and Application Repository . It contains all types of PHP code snippets and libraries. ]
p => [ It also provides a command line interface to install "packages" automatically. ]
h3 => [ 3) Who is known as the father of PHP? ]
em => [ Rasmus Lerdorf ]
h3 => [ 4) What was the old name of PHP? ]
p => [ The old name of PHP was Personal Home Page. ]
h3 => [ 5) Explain the difference b/w static and dynamic websites? ]
p => [  In  static websites ,  content can't be changed  after running the script. You can't change anything on the site. It is predefined. ]
p => [  In  dynamic websites ,  content of script can be changed at the run time . Its content is regenerated every time a user visit or reload. Google, yahoo and every search engine is the example of dynamic website. ]
h3 => [ 6) What is the name of scripting engine in PHP? ]
p => [  The scripting engine that powers PHP is called  Zend Engine 2 . ]
h3 => [ 7) Explain the difference between PHP4 and PHP5. ]
p => [ PHP4 doesn't support oops concept and uses Zend Engine 1. ]
p => [ PHP5 supports oops concept and uses Zend Engine 2. ]
h3 => [ 8) What are the popular Content Management Systems (CMS) in PHP? ]
strong => [ WordPress: ]
strong => [ Joomla: ]
strong => [ Magento: ]
strong => [ Drupal: ]
h3 => [ 9) What are the popular frameworks in PHP? ]
li => [ CakePHP ]
li => [ CodeIgniter ]
li => [ Yii 2 ]
li => [ Symfony ]
li => [ Zend Framework etc. ]
h3 => [ 10) Which programming language does PHP resemble to? ]
p => [ PHP has borrowed its syntax from Perl and C. ]
h3 => [ 11) List some of the features of PHP7. ]
li => [ Scalar type declarations ]
li => [ Return type declarations ]
li => [ Null coalescing operator (??) ]
li => [ Spaceship operator ]
li => [ Constant arrays using define() ]
li => [ Anonymous classes ]
li => [ Closure::call method ]
li => [ Group use declaration ]
li => [ Generator return expressions ]
li => [ Generator delegation ]
li => [ Space ship operator ]
h3 => [ 12) What is "echo" in PHP? ]
p => [ PHP echo output one or more string. It is a language construct not a function. So the use of parentheses is not required. But if you want to pass more than one parameter to echo, the use of parentheses is required. ]
strong => [ Syntax: ]
a => [ More details... ]
h3 => [ 13) What is "print" in PHP? ]
p => [ PHP print output a string. It is a language construct not a function. So the use of parentheses is not required with the argument list. Unlike echo, it always returns 1. ]
strong => [ Syntax: ]
a => [ More details... ]
h3 => [ 14) What is the difference between "echo" and "print" in PHP? ]
p => [  Echo  can  output one or more string  but  print  can only  output one string and always returns 1 . ]
p => [  Echo  is  faster than print  because it does not return any value. ]
h3 => [ 15) How a variable is declared in PHP? ]
p => [ A PHP variable is the name of the memory location that holds data. It is temporary storage. ]
strong => [ Syntax: ]
a => [ More details... ]
h3 => [ 16) What is the difference between $message and $$message? ]
p => [  $message   stores variable data  while  $$message  is used  to store variable of variables .  ]
p => [ $message stores fixed data whereas the data stored in $$message may be changed dynamically. ]
a => [ More Details... ]
h3 => [ 17) What are the ways to define a constant in PHP? ]
p => [ PHP constants are name or identifier that can't be changed during execution of the script. PHP constants are defined in two ways: ]
li => [ Using define() function ]
li => [ Using const() function ]
a => [ More details... ]
h3 => [ 18) What are magic constants in PHP? ]
p => [ PHP magic constants are predefined constants, which change based on their use. They start with a double underscore (__) and end with a double underscore (__). ]
a => [ More Details... ]
h3 => [ 19) How many data types are there in PHP? ]
p => [ PHP data types are used to hold different types of data or values. There are 8 primitive data types which are further categorized in 3 types: ]
li => [ Scalar types ]
li => [ Compound types ]
li => [ Special types ]
a => [ More Details... ]
h3 => [ 20) How to do single and multi line comment in PHP? ]
p => [ PHP single line comment is made in two ways: ]
li => [ Using // (C++ style single line comment) ]
li => [ Using # (Unix Shell style single line comment) ]
p => [ PHP multi-line comment is made by enclosing all lines within. ]
a => [ More details... ]
h3 => [ 21) What are the different loops in PHP? ]
p => [ For, while, do-while and for each. ]
h3 => [ 22) What is the use of count() function in PHP? ]
p => [  The PHP count() function is used  to count total elements in the array, or something an object . ]
h3 => [ 23) What is the use of header() function in PHP? ]
p => [ The header() function is used to send a raw HTTP header to a client. It must be called before sending the actual output. For example, you can't print any HTML element before using this function. ]
h3 => [ 24) What does isset() function? ]
p => [ The isset() function checks if the variable is defined and not null. ]
h3 => [ 25) Explain PHP parameterized functions. ]
p => [ PHP parameterized functions are functions with parameters. You can pass any number of parameters inside a function. These given parameters act as variables inside your function. They are specified inside the parentheses, after the function name. Output depends upon dynamic values passed as parameters into the function. ]
a => [ More details... ]
h3 => [ 26) Explain PHP variable length argument function ]
p => [ PHP supports variable length argument function. It means you can pass 0, 1 or n number of arguments in function. To do this, you need to use 3 ellipses (dots) before the argument name. The 3 dot concept is implemented for variable length argument since PHP 5.6. ]
a => [ More details... ]
h3 => [ 27) Explain PHP variable length argument function. ]
p => [ PHP supports variable length argument function. It means you can pass 0, 1 or n number of arguments. ]
h3 => [ 28) What is the array in PHP? ]
p => [ An array is used to store multiple values in a single value. In PHP, it orders maps of pairs of keys and values. It saves the collection of the data type. ]
a => [ More Details... ]
h3 => [ 29) How many types of array are there in PHP? ]
p => [ There are three types of array in PHP: ]
strong => [ Indexed array: ]
strong => [ Associative array: ]
strong => [ Multidimensional array: ]
h3 => [ 30) Explain some of the PHP array functions? ]
p => [ There are many array functions in PHP: ]
li => [ array() ]
li => [ array_change_key_case() ]
li => [ array_chunk() ]
li => [ count() ]
li => [ sort() ]
li => [ array_reverse() ]
li => [ array_search() ]
li => [ array_intersect() ]
a => [ More details... ]
h3 => [ 31) What is the difference between indexed and associative array? ]
p => [ The indexed array holds elements in an indexed form which is represented by number starting from 0 and incremented by 1. For example: ]
p => [ The associative array holds elements with name. For example: ]
p => [  More Details... 
 ]
h3 => [ 32) How to get the length of string? ]
p => [ The strlen() function is used to get the length of the string. ]
a => [ More Details... ]
h3 => [ 33) Explain some of the PHP string functions? ]
p => [ There are many array functions in PHP: ]
li => [ strtolower() ]
li => [ strtoupper() ]
li => [ ucfirst() ]
li => [ lcfirst() ]
li => [ ucwords() ]
li => [ strrev() ]
li => [ strlen() ]
a => [ More details... ]
h3 => [ 34) What are the methods to submit form in PHP? ]
p => [ There are two methods GET and POST. ]
a => [ More Details... ]
h3 => [ 35) How can you submit a form without a submit button? ]
p => [ You can use JavaScript submit() function to submit the form without explicitly clicking any submit button. ]
h3 => [ 36) What are the ways to include file in PHP? ]
p => [ PHP allows you to include file so that page content can be reused again. There are two ways to add the file in PHP. ]
li => [ include ]
li => [ require ]
a => [ More details... ]
h3 => [ 37) Differentiate between require and include? ]
p => [  Require and include both are used to include a file, but if data is not found  include sends warning  whereas  require sends Fatal error . ]
a => [ More Details... ]
h3 => [ 38) Explain setcookie() function in PHP? ]
p => [ PHP setcookie() function is used to set cookie with HTTP response. Once the cookie is set, you can access it by $_COOKIE superglobal variable. ]
strong => [ Syntax: ]
a => [ More details... ]
h3 => [ 39) How can you retrieve a cookie value? ]
a => [ More Details... ]
h3 => [ 40) What is a session? ]
p => [ PHP Engine creates a logical object to preserve data across subsequent HTTP requests, which is known as session. ]
p => [ Sessions generally store temporary data to allow multiple PHP pages to offer a complete functional transaction for the same user. ]
p => [ Simply, it maintains data of an user (browser). ]
a => [ More Details... ]
h3 => [ 41) What is the method to register a variable into a session? ]
h3 => [ 42) What is $_SESSION in PHP? ]
p => [ A session creates a file in a temporary directory on the server where registered session variables and their session id are stored. This data will be available to all pages on the site amid that visit. ]
p => [ The area of the temporary record is controlled by a setting in the php.ini document called session.save_path.  ]
p => [ At the point when a session is begun following things happen - ]
li => [ PHP first makes two duplicates of one of a kind session id for that particular session of the client which is an arbitrary string of 32 hexadecimal numbers, for example, 3c7foj34c3jjhkyepop2fc937e3443. ]
li => [ One copy of unique session id automatically sent to the user?s computer for the sake of synchronization in future ahead, and one copy is being maintained at server side till the session is running. ]
li => [ Whenever you want to access the page of website or web app, then session id of the current user will be associated with the HTTP header, and that will be compared by the session id which is being maintained at the server. After completing the comparison process, you can easily access the page of the website or web app ]
li => [ A session ends when the user closes the browser, or after leaving the site, the server will terminate the session after a predetermined period, commonly 30 minutes duration. ]
h3 => [ 43) What is PHP session_start() and session_destroy() function? ]
p => [ PHP session_start() function is used to start the session. It starts new or resumes the current session. It returns the current session if the session is created already. If the session is not available, it creates and returns new sessions. ]
a => [ More details... ]
h3 => [ 44) What is the difference between session and cookie? ]
p => [  The main difference between session and cookie is that  cookies are stored on user's computer in the text file  format while  sessions are stored on the server side . ]
p => [ Cookies can't hold multiple variables, on the other hand, Session can hold multiple variables. ]
p => [ You can manually set an expiry for a cookie, while session only remains active as long as browser is open. ]
h3 => [ 45) Write syntax to open a file in PHP? ]
p => [ PHP fopen() function is used to open file or URL and returns resource. It accepts two arguments: $filename and $mode. ]
strong => [ Syntax: ]
a => [ More details... ]
h3 => [ 46) How to read a file in PHP? ]
p => [ PHP provides various functions to read data from the file. Different functions allow you to read all file data, read data line by line, and read data character by character. ]
p => [ PHP file read functions are given below: ]
li => [ fread() ]
li => [ fgets() ]
li => [ fgetc() ]
a => [ More details... ]
h3 => [ 47) How to write in a file in PHP? ]
p => [ PHP fwrite() and fputs() functions are used to write data into file. To write data into a file, you need to use w, r+, w+, x, x+, c or c+ mode. ]
a => [ More details... ]
h3 => [ 48) How to delete file in PHP? ]
p => [ The unlink() function is used to delete a file in PHP. ]
p => [  More Details... 
 ]
h3 => [ 49) What is the method to execute a PHP script from the command line? ]
p => [ You should just run the PHP command line interface (CLI) and specify the file name of the script to be executed as follows. ]
h3 => [ 50) How to upload file in PHP? ]
p => [ The move_uploaded_file() function is used to upload file in PHP. ]
p => [  More Details... 
 ]
h3 => [ 51) How to download file in PHP? ]
p => [ The readfile() function is used to download the file in PHP. ]
p => [  More Details... 
 ]
h3 => [ 52) How can you send email in PHP? ]
p => [ The mail() function is used to send email in PHP. ]
p => [  More Details... 
 ]
h3 => [ 53) How do you connect MySQL database with PHP? ]
p => [ There are two methods to connect MySQL database with PHP. Procedural and object-oriented style. ]
a => [ More Details... ]
h3 => [ 54) How to create connection in PHP? ]
p => [ The mysqli_connect() function is used to create a connection in PHP. ]
p => [  More Details... 
 ]
h3 => [ 55) How to create database connection and query in PHP? ]
p => [ Since PHP 4.3, mysql_reate_db() is deprecated. Now you can use the following 2 alternatives. ]
li => [ mysqli_query() ]
li => [ PDO::_query() ]
a => [ More details... ]
h3 => [ 56) How can we increase execution time of a PHP script? ]
p => [ By default, the maximum execution time for PHP scripts is set to 30 seconds. If a script takes more than 30 seconds, PHP stops the script and returns an error. ]
p => [ You can change the script run time by changing the max_execution_time directive in the php.ini file. ]
p => [ When a script is called, set_time_limit function restarts the timeout counter from zero. It means, if default timer is set to 30 sec, and 20 sec is specified in function set_time_limit(), then script will run for 45 seconds. If 0sec is specified in this function, script takes unlimited time. ]
h3 => [ 57) What are the different types of errors in PHP? ]
p => [ There are 3 types of error in PHP. ]
strong => [ Notices: ]
strong => [ Warnings: ]
strong => [ Fatal Errors: ]
h3 => [ 58) How to stop the execution of PHP script? ]
p => [ The exit() function is used to stop the execution of PHP script. ]
h3 => [ 59) What are the encryption functions in PHP? ]
p => [  CRYPT()  and  MD5() ]
h3 => [ 60) What is htaccess in PHP? ]
p => [ The .htaccess is a configuration file on Apache server. You can change configuration settings using directives in Apache configuration files like .htaccess and httpd.conf. ]
h3 => [ 61) Explain PHP explode() function. ]
p => [ The PHP explode() function breaks a string into an array. ]
h3 => [ 62) Explain PHP split() function. ]
p => [ The PHP split() function splits string into an array by regular expression. ]
h3 => [ 63) How can we get IP address of a client in PHP? ]
h3 => [ 64) What is the meaning of a Persistent Cookie? ]
p => [ A persistent cookie is permanently stored in a cookie file on the browser's computer. By default, cookies are temporary and are erased if we close the browser. ]
h3 => [ 65) What is the use of the function 'imagetypes()'? ]
p => [ imagetypes() gives the image format and types supported by the current version of GD-PHP. ]
h3 => [ 66) What are include() and require() functions? ]
p => [  The  Include()  function is used to put data of one PHP file into another PHP file. If errors occur, then the include() function produces a warning but does not stop the execution of the script, and it will continue to execute. ]
p => [  The  Require()  function is also used to put data of one PHP file to another PHP file. If there are any errors, then the require() function produces a warning and a fatal error and stops the script. ]
h3 => [ 67) What is Cookies? How to create cookies in PHP? ]
p => [ A cookie is used to identify a user. A cookie is a little record that the server installs on the client's Computer. Each time a similar PC asks for a page with a program, it will send the cookie as well. With PHP, you can both make and recover cookie value. ]
strong => [ Some important points regarding Cookies: ]
li => [ Cookies maintain the session id generated at the back end after verifying the user's identity in encrypted form, and it must reside in the browser of the machine ]
li => [ You can store only string values not object because you can't access any object across the website or web apps ]
li => [ Scope: - Multiple pages. ]
li => [ By default, cookies are temporary and transitory cookie saves in the browser only. ]
li => [ By default, cookies are URL particular means Gmail isn't supported in Yahoo and the vice versa. ]
li => [ Per site 20 cookies can be created in one website or web app ]
li => [ The Initial size of the cookie is 50 bytes. ]
li => [ The Maximum size of the cookie is 4096 bytes. ]
h3 => [ 68) What is the Importance of Parser in PHP? ]
p => [  PHP parser parses the PHP developed website from the opening to the closing tag. Tags indicate that from where PHP code is being started and ended. In other words, opening and closing tags decide the scope of PHP scripting syntax of closing tag in PHP ]
p => [  &lt;?php syntax of opening tag in PHP 
?&gt; syntax of closing tag in PHP ]
h3 => [ 69) How can we create a database using PHP and MySQL? ]
p => [ The necessary steps to create a MySQL database using PHP are: ]
strong => [ connection ]
strong => [ database ]
strong => [ Execute ]
a => [ Java Basics Interview Questions ]
a => [ Java OOPs Interview Questions ]
a => [ Java Multithreading Questions ]
a => [ Java String &amp; Exception Questions ]
a => [ Java Collection Interview Questions ]
a => [ JDBC Interview Questions ]
a => [ Servlet Interview Questions ]
a => [ JSP Interview Questions ]
span => [ Spring Interview Questions ]
span => [ Hibernate Interview Questions ]
span => [ PL/SQL Interview Questions ]
span => [ SQL Interview Questions ]
span => [ Oracle Interview Questions ]
span => [ Android Interview Questions ]
span => [ SQL Server Interview Questions ]
span => [ MySQL Interview Questions ]
h2 => [ You may also like: ]
a => [ Java Interview Questions ]
a => [ SQL Interview Questions ]
a => [ Python Interview Questions ]
a => [ JavaScript Interview Questions ]
a => [ Angular Interview Questions ]
a => [ Selenium Interview Questions ]
a => [ Spring Boot Interview Questions ]
a => [ HR Interview Questions ]
a => [ C Programming Interview Questions ]
a => [ C++ Interview Questions ]
a => [ Data Structure Interview Questions ]
a => [ DBMS Interview Questions ]
a => [ HTML Interview Questions ]
a => [ IAS Interview Questions ]
a => [ Manual Testing Interview Questions ]
a => [ OOPs Interview Questions ]
a => [ .Net Interview Questions ]
a => [ C# Interview Questions ]
a => [ ReactJS Interview Questions ]
a => [ Networking Interview Questions ]
a => [ PHP Interview Questions ]
a => [ CSS Interview Questions ]
a => [ Node.js Interview Questions ]
a => [ Spring Interview Questions ]
a => [ Hibernate Interview Questions ]
a => [ AWS Interview Questions ]
a => [ Accounting Interview Questions ]
h2 => [ Learn Latest Tutorials ]
p => [ Splunk ]
p => [ SPSS ]
p => [ Swagger ]
p => [ Transact-SQL ]
p => [ Tumblr ]
p => [ ReactJS ]
p => [ Regex ]
p => [ Reinforcement Learning ]
p => [ R Programming ]
p => [ RxJS ]
p => [ React Native ]
p => [ Python Design Patterns ]
p => [ Python Pillow ]
p => [ Python Turtle ]
p => [ Keras ]
h2 => [ Preparation ]
p => [ Aptitude ]
p => [ Reasoning ]
p => [ Verbal Ability ]
p => [ Interview Questions ]
p => [ Company Questions ]
h2 => [ Trending Technologies ]
p => [ Artificial Intelligence ]
p => [ AWS ]
p => [ Selenium ]
p => [ Cloud Computing ]
p => [ Hadoop ]
p => [ ReactJS ]
p => [ Data Science ]
p => [ Angular 7 ]
p => [ Blockchain ]
p => [ Git ]
p => [ Machine Learning ]
p => [ DevOps ]
h2 => [ B.Tech / MCA ]
p => [ DBMS ]
p => [ Data Structures ]
p => [ DAA ]
p => [ Operating System ]
p => [ Computer Network ]
p => [ Compiler Design ]
p => [ Computer Organization ]
p => [ Discrete Mathematics ]
p => [ Ethical Hacking ]
p => [ Computer Graphics ]
p => [ Software Engineering ]
p => [ Web Technology ]
p => [ Cyber Security ]
p => [ Automata ]
p => [ C Programming ]
p => [ C++ ]
p => [ Java ]
p => [ .Net ]
p => [ Python ]
p => [ Programs ]
p => [ Control System ]
p => [ Data Mining ]
p => [ Data Warehouse ]
