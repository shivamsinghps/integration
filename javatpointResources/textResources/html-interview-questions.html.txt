
div => [ 
 ]
h1 => [ HTML Interview Questions ]
p => [  A list of top frequently asked  HTML interview questions  and HTML5 interview questions and answers are given below.  ]
a => [ HTML5 Interview Questions ]
h3 => [ 1) What is HTML? ]
p => [  HTML stands for Hyper Text Markup Language. It is a language of World Wide Web. It is a standard text formatting language which is used to create and display pages on the Web. It makes the text more interactive and dynamic. It can turn text into images, tables, links.  More details. ]
h3 => [ 2) What are Tags? ]
p => [ HTML tags are composed of three things: an opening tag, content and ending tag. Some tags are unclosed tags. ]
p => [ HTML documents contain two things: ]
li => [ content, and ]
li => [ tags ]
p => [ When a web browser reads an HTML document, the browser reads it from top to bottom and left to right. HTML tags are used to create HTML documents and render their properties. Each HTML tags have different properties. ]
h4 => [ Syntax ]
p => [ Content is placed between tags to display data on the web page. ]
a => [ More details. ]
h3 => [ 3) Do all HTML tags have an end tag? ]
p => [  No. There are some HTML tags that don't need a closing tag. For example: &lt;image&gt; tag, &lt;br&gt; tag.  More details. ]
h3 => [ 4) What is formatting in HTML? ]
p => [  The HTML formatting is a process of format the text for a better look and feel. It uses different tags to make text bold, italicized, underlined.  More details. ]
h3 => [ 5) How many types of heading does an HTML contain? ]
p => [ The HTML contains six types of headings which are defined with the &lt;h1&gt; to &lt;h6&gt; tags. Each type of heading tag displays different text size from another. So, &lt;h1&gt; is the largest heading tag and &lt;h6&gt; is the smallest one. For example:  ]
a => [ More details. ]
h3 => [ 6) How to create a hyperlink in HTML? ]
p => [ The HTML provides an anchor tag to create a hyperlink that links one page to another page. These tags can appear in any of the following ways: ]
li => [ Unvisited link - It is displayed, underlined and blue. ]
li => [ Visited link - It is displayed, underlined and purple. ]
li => [ Active link - It is displayed, underlined and red. ]
a => [ More details. ]
h3 => [ 7) Which HTML tag is used to display the data in the tabular form? ]
p => [  The  HTML table tag  is used to display data in tabular form (row * column). It also manages the layout of the page, e.g., header section, navigation bar, body content, footer section. Here is the list of tags used while displaying the data in the tabular form:  ]
th => [ Tag ]
th => [ Description ]
td => [ &lt;table&gt; ]
td => [ It defines a table. ]
td => [ &lt;tr&gt; ]
td => [ It defines a row in a table. ]
td => [ &lt;th&gt; ]
td => [ It defines a header cell in a table. ]
td => [ &lt;td&gt; ]
td => [ It defines a cell in a table. ]
td => [ &lt;caption&gt; ]
td => [ It defines the table caption. ]
td => [ &lt;colgroup&gt; ]
td => [ It specifies a group of one or more columns in a table for formatting. ]
td => [ &lt;col&gt; ]
td => [ It is used with &lt;colgroup&gt; element to specify column properties for each
column. ]
td => [ &lt;tbody&gt; ]
td => [ It is used to group the body content in a table. ]
td => [ &lt;thead&gt; ]
td => [ It is used to group the header content in a table. ]
td => [ &lt;tfooter&gt; ]
td => [ It is used to group the footer content in a table. ]
h3 => [ 8) What are some common lists that are used when designing a page? ]
p => [ There are many common lists which are used to design a page. You can choose any or a combination of the following list types: ]
li => [ Ordered list - The ordered list displays elements in numbered format. It is represented by &lt;ol&gt; tag. ]
li => [ Unordered list - The unordered list displays elements in bulleted format. It is represented by &lt;ul&gt; tag. ]
li => [ Definition list - The definition list displays elements in definition form like in dictionary. The &lt;dl&gt;, &lt;dt&gt; and &lt;dd&gt; tags are used to define description list. ]
a => [ More details. ]
h3 => [ 9) What is the difference between HTML elements and tags? ]
p => [ HTML elements communicate to the browser to render text. When the elements are enclosed by brackets &lt;&gt;, they form HTML tags. Most of the time, tags come in a pair and surround content. ]
h3 => [ 10) What is semantic HTML? ]
p => [ Semantic HTML is a coding style. It is the use of HTML markup to reinforce the semantics or meaning of the content. For example: In semantic HTML &lt;b&gt; &lt;/b&gt; tag is not used for bold statement as well as &lt;i&gt; &lt;/i&gt; tag is used for italic. Instead of these we use &lt;strong&gt;&lt;/strong&gt; and &lt;em&gt;&lt;/em&gt; tags. ]
h3 => [ 11) What is an image map? ]
p => [ Image map facilitates you to link many different web pages using a single image. It is represented by &lt;map&gt; tag. You can define shapes in images that you want to make part of an image mapping. ]
h3 => [ 12) How to insert a copyright symbol on a browser page? ]
p => [ You can insert a copyright symbol by using &amp;copy; or &amp;#169; in an HTML file.  ]
h3 => [ 13) How to create a nested webpage in HTML? ]
p => [ The HTML iframe tag is used to display a nested webpage. In other words, it represents a webpage within a webpage. The HTML &lt;iframe&gt; tag defines an inline frame. For example: ]
h3 => [ 14) How do you keep list elements straight in an HTML file? ]
p => [ You can keep the list elements straight by using indents.  ]
h3 => [ 15) Does a hyperlink only apply to text? ]
p => [ No, you can use hyperlinks on text and images both. The HTML anchor tag defines a hyperlink that links one page to another page. The "href" attribute is the most important attribute of the HTML anchor tag.
 ]
h4 => [ Syntax ]
a => [ More details. ]
h3 => [ 16) What is a style sheet? ]
p => [ A style sheet is used to build a consistent, transportable, and well-designed style template. You can add these templates on several different web pages. It describes the look and formatting of a document written in markup language.  ]
h3 => [ 17) Can you create a multi-colored text on a web page? ]
p => [ Yes. To create a multicolor text on a web page you can use &lt;font color ="color"&gt; &lt;/font&gt; for the specific texts you want to color.  ]
h3 => [ 18) Is it possible to change the color of the bullet? ]
p => [ The color of the bullet is always the color of the first text of the list. So, if you want to change the color of the bullet, you must change the color of the text.  ]
h3 => [ 19) Explain the layout of HTML? ]
p => [ HTML layout specifies a way in which the web page is arranged. ]
p => [ Every website has a specific layout to display content in a specific manner.  ]
p => [ Following are different HTML5 elements which are used to define the different parts of a webpage.  ]
li => [ &lt;header&gt;: It is used to define a header for a document or a section. ]
li => [ &lt;nav&gt;: It is used to define a container for navigation links ]
li => [ &lt;section&gt;: It is used to define a section in a document ]
li => [ &lt;article&gt;: It is used to define an independent, self-contained article ]
li => [ &lt;aside&gt;: It is used to define content aside from the content (like a sidebar) ]
li => [ &lt;footer&gt;: It is used to define a footer for a document or a section ]
h3 => [ 20) What is a marquee? ]
p => [  Marquee is used to put the scrolling text on a web page. It scrolls the image or text up, down, left or right automatically. You should put the text which you want to scroll within the &lt;marquee&gt;......&lt;/marquee&gt; tag.  More details. ]
h3 => [ 21) How many tags can be used to separate a section of texts? ]
p => [ Three tags are used to separate the texts.  ]
li => [ &lt;br&gt; tag - Usually &lt;br&gt; tag is used to separate the line of text. It breaks the current line and conveys the flow to the next line ]
li => [ &lt;p&gt; tag - The &lt;p&gt; tag contains the text in the form of a new paragraph.  ]
li => [ &lt;blockquote&gt; tag - It is used to define a large quoted section. If you have a large quotation, then put the entire text within &lt;blockquote&gt;.............&lt;/blockquote&gt; tag. ]
h3 => [ 22) How to make a picture of a background image of a web page? ]
p => [ To make a picture a background image on a web page, you should put the following tag code after the &lt;/head&gt; tag. ]
p => [ Here, replace the "image.gif" with the name of your image file which you want to display on your web page.  ]
h3 => [ 23) What are empty elements? ]
p => [ HTML elements with no content are called empty elements. For example: &lt;br&gt;, &lt;hr&gt; etc. ]
h3 => [ 24) What is the use of a span tag? Give one example. ]
p => [ The span tag is used for following things: ]
li => [ For adding color on text ]
li => [ For adding background on text ]
li => [ Highlight any color text  ]
strong => [ Example: ]
h3 => [ 25) What is the use of an iframe tag? ]
p => [ An iframe is used to display a web page within a web page. ]
strong => [ Syntax: ]
strong => [ Example: ]
strong => [ Target to a link: ]
h3 => [ 26) What are the entities in HTML? ]
p => [ The HTML character entities are used as a replacement for reserved characters in HTML. You can also replace characters that are not present on your keyboard by entities. These characters are replaced because some characters are reserved in HTML. ]
h3 => [ 27) Why is a URL encoded in HTML? ]
p => [ An URL is encoded to convert non-ASCII characters into a format that can be used over the Internet because a URL is sent over the Internet by using the ASCII character-set only. If a URL contains characters outside the ASCII set, the URL has to be converted. The non-ASCII characters are replaced with a "%" followed by hexadecimal digits. ]
h3 => [ 28) Does a &lt;!DOCTYPE html&gt; tag is a HTML tag? ]
p => [ No, the &lt;!DOCTYPE html&gt; declaration is not an HTML tag. There are many type of HTML e.g. HTML 4.01 Strict, HTML 4.01 Transitional, HTML 4.01 Frameset, XHTML 1.0 Strict, XHTML 1.0 Transitional, XHTML 1.0 Frameset, XHTML 1.1 etc. So, &lt;!DOCTYPE html&gt; is used to instruct the web browser about the HTML page. ]
h2 => [ HTML5 Interview Questions ]
p => [ Let's see a list of top HTML5 interview questions and answers. ]
h3 => [ 29) What is the canvas element in HTML5? ]
p => [ The &lt;canvas&gt; element is a container that is used to draw graphics on the web page using scripting language like JavaScript. It allows for dynamic and scriptable rendering of 2D shapes and bitmap images. There are several methods in canvas to draw paths, boxes, circles, text and add images. For Example: ]
a => [ More details. ]
h3 => [ 30) What is SVG? ]
p => [ HTML SVG is used to describe the two-dimensional vector and vector/raster graphics. SVG images and their behaviors are defined in XML text files. So as XML files, you can create and edit an SVG image with the text editor. It is mostly used for vector type diagrams like pie charts, 2-Dimensional graphs in an X, Y coordinate system.
 ]
a => [ More details. ]
h3 => [ 31) What are the different new form element types in HTML 5? ]
p => [ Following is a list of 10 frequently used new elements in HTML 5: ]
li => [ Color ]
li => [ Date ]
li => [ Datetime-local ]
li => [ Email ]
li => [ Time ]
li => [ Url ]
li => [ Range ]
li => [ Telephone ]
li => [ Number ]
li => [ Search ]
h3 => [ 32) Is there any need to change the web browsers to support HTML5? ]
p => [ No. Almost all browsers (updated versions) support HTML 5. For example Chrome, Firefox, Opera, Safari, IE. ]
h3 => [ 33) Which type of video formats are supported by HTML5? ]
p => [ HTML 5 supports three types of video format: ]
li => [ mp4 ]
li => [ WebM ]
li => [ Ogg ]
a => [ More details. ]
h3 => [ 34) Is audio tag supported in HTML 5? ]
p => [ Yes. It is used to add sound or music files on the web page. There are three
supported file formats for HTML 5 audio tag. ]
li => [ mp3 ]
li => [ WAV ]
li => [ Ogg ]
p => [ Let's see the code to play mp3 file using HTML audio tag.  ]
p => [ Instead of koyal.mp3, you can pass any mp3 file name. ]
a => [ More details. ]
h3 => [ 35) What is the difference between progress and meter tag? ]
p => [  The progress tag is used to represent the progress of the task only while the meter tag is used to measure data within a given range.  More details. ]
h3 => [ 36) What is the use of figure tag in HTML 5? ]
p => [ The figure tag is used to add a photo in the document on the web page. It is used to handle the group of diagrams, photos, code listing with some embedded content. ]
a => [ More details. ]
h3 => [ 37) What is the use of figcaption tag in HTML 5? ]
p => [ The &lt;figcaption&gt; element is used to provide a caption to an image. It is an optional tag and can appear before or after the content within the &lt;figure&gt; tag. The &lt;figcaption&gt; element is used with &lt;figure&gt; element and it can be placed as the first or last child of the &lt;figure&gt; element. ]
a => [ More details. ]
h3 => [ 38) What is button tag? ]
p => [ The button tag is used in HTML 5. It is used to create a clickable button within the HTML form on the web page. It is generally used to create a "submit" or "reset" button. Let's see the code to display the button. ]
a => [ More details. ]
h3 => [ 39) What is the use of details and summary tag? ]
p => [  The details tag is used to specify some additional details on the web page. It can be viewed or hidden on demand. The summary tag is used with details tag.  More details. ]
h3 => [ 40) What is datalist tag? ]
p => [ The HTML 5 datalist tag provides an autocomplete feature on the form element. It facilitates users to choose the predefined options to the users to select data. ]
a => [ More details. ]
h3 => [ 41) How are tags migrated from HTML4 to HTML5? ]
th => [ No. ]
th => [ Typical HTML4 ]
th => [ Typical HTML5 ]
td => [ 1) ]
td => [ &lt;div id="header"&gt; ]
td => [ &lt;header&gt; ]
td => [ 2) ]
td => [ &lt;div id="menu"&gt; ]
td => [ &lt;nav&gt; ]
td => [ 3) ]
td => [ &lt;div id="content"&gt; ]
td => [ &lt;section&gt; ]
td => [ 4) ]
td => [ &lt;div id="post"&gt; ]
td => [ &lt;article&gt; ]
td => [ 5) ]
td => [ &lt;div id="footer"&gt; ]
td => [ &lt;footer&gt; ]
h3 => [ Header and Footer Example ]
strong => [ HTML 4 Header and Footer: ]
strong => [ HTML 5 Header and Footer: ]
h3 => [ Menu Example ]
strong => [ HTML 4 Menu: ]
strong => [ HTML 5 Menu: ]
h3 => [ 42) If I do not put &lt;!DOCTYPE html&gt; will HTML 5 work? ]
p => [ No, the browser will not be able to identify that it is an HTML document and HTML 5 tags do not function properly.. ]
h3 => [ 43) What is the use of the required attribute in HTML5? ]
p => [ It forces a user to fill text on the text field or text area before submitting the form. It is used for form validation. ]
strong => [ Example: ]
h3 => [ 44) What are the new &lt;input&gt; types for form validation in HTML5? ]
p => [ The new input types for form validation are email, URL, number, tel, and date. ]
strong => [ Example: ]
a => [ Interview Tips ]
a => [ Job/HR Interview Questions ]
a => [ JavaScript Interview Questions ]
a => [ jQuery Interview Questions ]
a => [ Java Basics Interview Questions ]
a => [ Java OOPs Interview Questions ]
a => [ Servlet Interview Questions ]
a => [ JSP Interview Questions ]
span => [ Spring Interview Questions ]
span => [ Hibernate Interview Questions ]
span => [ PL/SQL Interview Questions ]
span => [ SQL Interview Questions ]
span => [ Oracle Interview Questions ]
span => [ Android Interview Questions ]
span => [ SQL Server Interview Questions ]
span => [ MySQL Interview Questions ]
h2 => [ You may also like: ]
a => [ Java Interview Questions ]
a => [ SQL Interview Questions ]
a => [ Python Interview Questions ]
a => [ JavaScript Interview Questions ]
a => [ Angular Interview Questions ]
a => [ Selenium Interview Questions ]
a => [ Spring Boot Interview Questions ]
a => [ HR Interview Questions ]
a => [ C Programming Interview Questions ]
a => [ C++ Interview Questions ]
a => [ Data Structure Interview Questions ]
a => [ DBMS Interview Questions ]
a => [ HTML Interview Questions ]
a => [ IAS Interview Questions ]
a => [ Manual Testing Interview Questions ]
a => [ OOPs Interview Questions ]
a => [ .Net Interview Questions ]
a => [ C# Interview Questions ]
a => [ ReactJS Interview Questions ]
a => [ Networking Interview Questions ]
a => [ PHP Interview Questions ]
a => [ CSS Interview Questions ]
a => [ Node.js Interview Questions ]
a => [ Spring Interview Questions ]
a => [ Hibernate Interview Questions ]
a => [ AWS Interview Questions ]
a => [ Accounting Interview Questions ]
h2 => [ Learn Latest Tutorials ]
p => [ Splunk ]
p => [ SPSS ]
p => [ Swagger ]
p => [ Transact-SQL ]
p => [ Tumblr ]
p => [ ReactJS ]
p => [ Regex ]
p => [ Reinforcement Learning ]
p => [ R Programming ]
p => [ RxJS ]
p => [ React Native ]
p => [ Python Design Patterns ]
p => [ Python Pillow ]
p => [ Python Turtle ]
p => [ Keras ]
h2 => [ Preparation ]
p => [ Aptitude ]
p => [ Reasoning ]
p => [ Verbal Ability ]
p => [ Interview Questions ]
p => [ Company Questions ]
h2 => [ Trending Technologies ]
p => [ Artificial Intelligence ]
p => [ AWS ]
p => [ Selenium ]
p => [ Cloud Computing ]
p => [ Hadoop ]
p => [ ReactJS ]
p => [ Data Science ]
p => [ Angular 7 ]
p => [ Blockchain ]
p => [ Git ]
p => [ Machine Learning ]
p => [ DevOps ]
h2 => [ B.Tech / MCA ]
p => [ DBMS ]
p => [ Data Structures ]
p => [ DAA ]
p => [ Operating System ]
p => [ Computer Network ]
p => [ Compiler Design ]
p => [ Computer Organization ]
p => [ Discrete Mathematics ]
p => [ Ethical Hacking ]
p => [ Computer Graphics ]
p => [ Software Engineering ]
p => [ Web Technology ]
p => [ Cyber Security ]
p => [ Automata ]
p => [ C Programming ]
p => [ C++ ]
p => [ Java ]
p => [ .Net ]
p => [ Python ]
p => [ Programs ]
p => [ Control System ]
p => [ Data Mining ]
p => [ Data Warehouse ]
