
div => [ 
 ]
h1 => [ Yii Interview Questions ]
p => [ The following are the most popular and useful Yii framework interview questions and answers for fresher and experienced candidates. These questions are created specifically to familiarize you with the types of questions you might encounter during your interview. According to our experiences, good interviewers rarely plan to ask any specific topic during the interview. Instead, questioning usually begins with a basic understanding of the subject, and based on your responses, further discussion happened. ]
h3 => [ 1) What is Yii? ]
p => [ Yii is an open-source, high-performance, component-based, and object-oriented PHP web application framework. Yii follows the MVC (Model-View-Controller) architectural pattern. This PHP framework is best for Web 2.0 development. ]
p => [ Yii enables maximum reusability in programming and significantly accelerates the process of web application development. ]
h3 => [ 2) What are Yii Versions? ]
p => [ The Yii framework currently has two major versions, one is 1.1, and another one is 2.0. Version 1.0 is old, and version 2.0 is a complete rewrite of Yii 1.0 ]
h3 => [ 3) Why is Yii so fast? ]
p => [ Yii is so much faster because it is using the lazy loading technique extensively. For example, it does not include a class file until the class is used for the first time. And it does not create an object until the object is accessed for the first time. Other frameworks suffer from the performance hit because they enable functionality (e.g., DB connection, user session) no matter if it is used or not during a request. ]
h3 => [ 4) What is Yii best for? ]
p => [ Yii is a generic Web programming framework that can be used for developing virtually any type of Web application. Because it is lightweight and equipped with sophisticated caching mechanisms, it is especially suited to high-traffic applications, such as portals, forums, content management systems (CMS), e-commerce systems, etc. ]
h3 => [ 5) Why Use Yii 2.0 Framework? ]
p => [ The Yii 2.0 Framework helps to create modern web applications quickly and make sure they perform well. Yii 2.0 framework is: ]
li => [ Easy to Install ]
li => [ Utilizes Modern Technologies ]
li => [ Highly Extensible ]
li => [ Encourages Testing ]
li => [ Simplifies Security ]
li => [ Shorten Development Time ]
li => [ Easy to Tune for Better Performance ]
h3 => [ 6) What are the main features of the Yii framework? ]
p => [ Yii framework has the following main features, such as: ]
li => [ Model-View-Controller (MVC) design pattern ]
li => [ Database Access Objects (DAO), Query Builder, Active Record, and DB Migration ]
li => [ Form input and validation ]
li => [ AJAX-enabled widgets ]
li => [ Yii provides a skinning and theming mechanism ]
li => [ Web Service available for Apps like android ]
li => [ Internationalization and localization translation for multilingual ]
li => [ Caching to speed up the application ]
li => [ Error handling and logging for tracking ]
li => [ It provides cross-site scripting (XSS), cross-site request forgery (CSRF) protection. ]
li => [ PHPUnit and Selenium for testing ]
li => [ Automatic code generation help us to fast development ]
li => [ Authentication and authorization ]
li => [ Friendly with third-party code ]
li => [ Extension library ]
h3 => [ 7) What's new in Yii version 2.0? ]
p => [ The following things are newly added in Yii version 2.0: ]
li => [ The Yii 2.0 framework supports PSR-4 class autoloading, simpler namespaces, faster loading, and improved usability for developers. ]
li => [ The Yii 2.0 framework added performance and security improvements. ]
li => [ The Yii 2.0 framework added RESTful API integration. ]
li => [ The Yii 2.0 framework added improvement on URL handling and processing. ]
li => [ Now Yii 2.0 framework, Translations of core messages available in 26 languages. ]
h3 => [ 8) What are Components in Yii? ]
p => [ Components are an independent set of code written to perform a specific task in controllers. Yii applications are built upon components which are objects written to a specification. ]
p => [ A component is an instance of a CComponent or its derived class. Using a component mainly involves accessing its properties and raising/handling its events. The base class CComponent specifies how to define properties and events. ]
h3 => [ 9) What are the core components of Yii? ]
p => [ Here are the following core components of Yii, such as: ]
strong => [ db- ]
strong => [ assetManager - ]
strong => [ authManager - ]
strong => [ cache- ]
strong => [ clientScript- ]
strong => [ coreMessages- ]
strong => [ errorHandler- ]
strong => [ themeManager- ]
strong => [ urlManager- ]
strong => [ statePersister- ]
strong => [ session- ]
strong => [ securityManager- ]
h3 => [ 10) What are the server requirements to install Yii? ]
p => [ Yii framework requires the following things, such as: ]
li => [ PHP 5.4 or above ]
li => [ mbstring extension ]
li => [ PCRE-support ]
h3 => [ 11) How to install the Yii framework? ]
p => [ You can install Yii2 by running these commands via composer: ]
h3 => [ 12) What is the directory structure of the Yii framework? ]
h3 => [ 13) What is Controller in the Yii framework? ]
p => [ Controllers are the glue that binds models, views, and other components together into a runnable application. Controllers are responsible for dealing directly with end-user requests. Therefore, ]
li => [ Controllers may access $_GET, $_POST, and other PHP variables representing user requests. ]
li => [ It may create model instances and manage their life cycles. Note that the actual implementation of saving a model should be located in the model instead of the controller. ]
li => [ It should avoid containing embedded SQL statements, which are better kept in models. ]
li => [ It should avoid containing any HTML or any other presentational markup, which is better kept in views. ]
h3 => [ 14) What is Model in Yii? ]
p => [ Models represent the underlying data structure of a Web application. Models are often shared among different sub-applications of a Web application. For example, a LoginForm model may be used by both the front end and the back end of an application. A New model may be used by the console commands, Web APIs, and the front or back end of an application. Therefore, ]
li => [ Models should contain properties to represent specific data. ]
li => [ It should contain business logic (e.g., validation rules) to ensure the represented data fulfills the design requirement. ]
li => [ It may contain code for manipulating data. ]
h3 => [ 15) What are Views in Yii? ]
p => [ Views are responsible for presenting models in the format that end users desire. In general, ]
li => [ Views should mainly contain presentational code, such as HTML and simple PHP code to traverse, format, and render data. ]
li => [ It should avoid containing code that performs explicit DB queries. Such code is better placed in models. ]
strong => [ $_GET, $_POST ]
li => [ It may access properties and methods of controllers and models directly. However, It should be done only for presentation. ]
h3 => [ 16) What are Yii Helpers? ]
p => [ Helpers are static classes in Yii that simplify common coding tasks, such as string or array manipulations, HTML code generation, etc. In Yii, all helpers are kept under the yii\helpers namespace. ]
p => [ You use a helper class in Yii by directly calling one of its static methods, as the following: ]
h3 => [ 17) What is Formatter in Yii? ]
p => [ Formatter is a Yii application component that is used format view data in readable format for users. By default, the formatter is implemented by yii\i18n\Formatter, which provides a set of methods to format data as date/time, numbers, currencies, and other commonly used formats. You can use the formatter as the following, ]
h3 => [ 18) What is Active Record (AR) in Yii? ]
p => [ Active Record provides an object-oriented interface for accessing and manipulating data stored in databases. An Active Record class is associated with a database table, an Active Record instance corresponds to a row of that table, and an attribute of an Active Record instance represents the value of a particular column in that row. ]
p => [ Instead of writing raw SQL statements, you would access Active Record attributes and call Active Record methods to access and manipulate the data stored in database tables. ]
h3 => [ 19) What is Gii in Yii2? ]
p => [ Gii is a web-based code-generator module provided by the Yii framework. It helps to create and generate fully customized forms, models, CRUD for databases, and more. ]
p => [ We can enable Gii by configuring it in the modules property of the application. Depending upon how we created our application. You may find the following code is already provided in the config/web.php configuration file: ]
h3 => [ 20) What are the benefits of Yii over other frameworks? ]
p => [ Yii has the following benefits over other frameworks of PHP: ]
li => [ The Yii framework is a generic Web programming framework used for developing all kinds of Web applications quickly using PHP. It is also very suitable for developing large-scale applications. ]
li => [ The primary goal of the Yii or Yii 2.0 framework is - high performance. ]
li => [ The Yii framework is a full-stack framework and ready-to-use features for both the relational and NoSQL databases. ]
li => [ The Yii framework implements the MVC (Model-View-Controller) architectural pattern. ]
li => [ The Yii framework is extremely extensible. ]
li => [ The Yii framework contains the Larger Community, and it also purchased an unlimited license to the beautiful web-based rich text editor and Redactor. ]
h3 => [ 21) What is the first file that loaded when you run an application using Yii? ]
p => [ index.php is the first file that loaded when we run an application using the Yii application. It will create a new object of new yii\web\Application and start the application. ]
h3 => [ 22) What is the first function that is loaded from a Controller? ]
p => [  index:  is the first function that gets loaded from a controller. ]
h3 => [ 23) What is the naming convention in Yii Framework? ]
p => [ In the latest version of Yii Framework uses a simple standard to set names. ]
li => [ You can define the table prefix when using Gii. In your case, you need to set it to tbl_. Then it should generate UserController instead of TblUserController. ]
li => [ The Yii Framework employs a class naming convention whereby the names of the classes directly map to the directories in which they are stored. The root-level directory of the Yii Framework is the "framework/" directory, under which all classes are stored hierarchically. ]
li => [ Class names may only contain alphanumeric characters. Numbers are permitted in class names but are discouraged. A dot (.) is only permitted in place of the path separator. ]
p => [ Routes that refer respectively to module, controller, and the action requested take the following format: ]
strong => [ ModuleID/ControllerID/ActionID ]
li => [ The ModuleID is optional. The ModuleID must be specified in the module's configuration property under the same name. ]
li => [ The ControllerID and ActionID should contain only English characters in lowercase, digits, underscores, dashes, and forward slashes. ]
li => [ URL Conventions: By default, Yii recognizes URLs with the following format: ]
p => [ The r refers to the route. ]
h3 => [ 24) What is CModel class in Yii? ]
p => [ Yii CModel is the base class that provides the common features needed by data model objects. CModel defines the basic framework for data models that need to be validated. All Models in Yii extend CModel class. ]
h3 => [ 25) Explain request Life Cycle in Yii2 framework? ]
p => [ The Request Life Cycle in the Yii2 framework explains in the following stages: ]
li => [ Pre-initialize the application with CApplication::preinit(); ]
li => [ Set up the error handling; ]
li => [ Register core application components; ]
li => [ Load application configuration; ]
li => [ Register application behaviors; ]
li => [ Load static application components; ]
li => [ Raise an onBeginRequest event; ]
li => [ Collect information about the request; ]
li => [ Create a controller; ]
li => [ Run the controller; ]
li => [ Raise an onEndRequest event; ]
h3 => [ 26) What is the difference between render, renderPartial and renderfile in the Yii framework? ]
th => [ Render() ]
th => [ renderPartial() ]
th => [ renderFile() ]
strong => [ render() ]
strong => [ renderPartial() ]
strong => [ renderFile() ]
h3 => [ 27) How can we use Ajax in Yii? ]
p => [ Ajax can be called in the Yii framework. The basic procedure to do so is: ]
li => [ Write the code that responds to (processes) the ajax call as an action in a controller. ]
li => [ Call the action with the (Yii-framework) appropriate url in the jQuery method.  ]
h3 => [ 28) What is meant habtm? ]
p => [ Habtm means HasAndBelongsToMany. It is a kind of association defined in models for retrieving related data across different entities. ]
h3 => [ 29) What is a Yiibase? ]
p => [ YiiBase is a helper class that serves functionalities of a common framework. It would be best if you did not use YiiBase directly. Instead, have to use its child class where we can customize the methods of YiiBase. ]
h3 => [ 30) How to generate CRUD code? ]
p => [ After creating a model class file, we shall generate the code that implements the CRUD operations for user data. We choose the Crud Generator in Gii. In the model class field, we enter 'User'. In the Controller ID field, we enter 'user' in lower case. Then press the preview button followed by the Generate button. Now we are done with the CRUD code generation. ]
h3 => [ 31) How to configure the Yii application with a database? ]
p => [ In the default application of Yii, open the main.php file that exists in protected/config/main.php and search for a parameter named db, there you can add host-name, database name, username, and password of your database server. ]
h3 => [ 32) How can you create and work with the different modules in Yii? ]
p => [ To create a module, access the gii url, and there you can create a module. After creating a module, you need to add it to the main configuration file of your Yii application. ]
h3 => [ 33) What are the Core Helper Classes in the Yii framework? ]
p => [ The following core helper classes are: ]
li => [ ArrayHelper ]
li => [ Console ]
li => [ FileHelper ]
li => [ FormatConverter ]
li => [ Html ]
li => [ HtmlPurifier ]
li => [ Imagine ]
li => [ Inflector ]
li => [ Json ]
li => [ Markdown ]
li => [ StringHelper ]
li => [ Url ]
li => [ VarDumper ]
h3 => [ 34) How to customizing Helper Classes in the Yii framework? ]
p => [ To customize a core helper class, we should create a new class extending from the helper base class. For example, ]
h3 => [ 35) How to set or change the default controller in Yii? ]
p => [ You can set or change the default controller in Yii by using the default yii2 advanced template that redirects to URL: ]
p => [ 1) Add below code on backend main/config.php user component: ]
p => [ 2) Yii2 advanced template default controller is controllers/siteController. You can change it to your module or controller name by putting the below configuration in main/config.php: ]
h3 => [ 36) What is CFormModel in Yii framework? ]
p => [ In Yii, CFormModel represents a data model that collects HTML form inputs. The data collected by CFormModel are stored in memory only instead of a database. Therefore to collect user inputs, you may extend CFormModel and define the attributes whose values are collected from user inputs. ]
h3 => [ 37) How to get and set session in Yii? ]
p => [ To set session variables, you will need to apply a global PHP $_SESSION variable. Whereas to access the data stored in a session, you can do the following: ]
h3 => [ 38) How to convert debug mode to production mode on the Yii project? ]
p => [ Just define YII_DEBUG as false on the entry script (root/index.php). ]
h3 => [ 39) What is CActiveRecord in the Yii framework? ]
p => [ Active Record (AR) is a design pattern used to abstract database access in an object-oriented fashion. Each AR object is an instance of CActiveRecord or a subclass of that class, representing a single row in a database table. ]
p => [ The fields in the row are represented as properties of the AR object. All AR details are found in Active Record. ]
h3 => [ 40) How to get the current url in Yii? ]
p => [ Yii::app()-&gt;request-&gt;getUrl() method is used to get current url in Yii framework. ]
h3 => [ 41) How to set Home page Url in Yii? ]
p => [ You can use the below code to set the Home Page Url: ]
h3 => [ 42) How many types of table relations are supported by Yii? ]
p => [ Yii supports almost every type of table relations that are used all across its platform. ]
h3 => [ 43) What is the use of the renderFile() function in Yii? ]
p => [ In Yii, the renderFile() function is usually used in response to AJAX Web requests. It renders a view specified in terms of a view file path or alias. ]
h3 => [ 44) How to get current controller name? ]
h3 => [ 45) How to get current action name? ]
h3 => [ 46) Which ORM does Yii2 supports? ]
p => [ Active Record provides an object-oriented interface for accessing and manipulating data stored in databases. ]
h3 => [ 47) What is another controller view in Yii? ]
p => [ By using renderPartial() method, You can call the view of another controller. You can reuse view files. You can call function in view and controller files. For example, ]
h3 => [ 48) What is the difference between Yii &amp; Yii2? ]
p => [ The difference between Yii &amp; Yii2 is explained as: ]
th => [ Yii ]
th => [ Yii2 ]
td => [ Yii requires PHP 5.2. ]
td => [ Yii 2.0 requires PHP 5.4.0 or higher, which makes use of the latest features of PHP. ]
td => [ In Yii, prefix C was used, and the classes were in global namespaces. ]
td => [ In Yii2, prefix C is not used in namespaces. And classes based on the directory structure. ]
td => [ Yii uses the On-event method, where custom event names are not allowed to use. ]
td => [ In Yii 2.0, any name can be used for the event with a handler attached to it, and event handling can be done using J-query. ]
td => [ Yii uses PHP as the primary template language. ]
td => [ Yii 2.0 is equipped with PHP and two more template engines- TWIG and SMARTY. ]
h3 => [ 49) What is a filter on the Yii framework? ]
p => [ The filter is a piece of code that is configured to be executed before or after a controller action executes. ]
p => [ An action can have multiple filters. The filters are executed in the order that they appear in the filter list. A filter can prevent the execution of the action and the rest of the unexecuted filters. ]
p => [ A filter can be defined as a controller class method. The method name must begin with a filter. ]
h3 => [ 50) What are the types of models available in the Yii framework? ]
p => [ Yii implements two kinds of models. And they both extend from the same base class, CModel. ]
li => [ Form models ]
li => [ Active records. ]
h2 => [ You may also like: ]
a => [ Java Interview Questions ]
a => [ SQL Interview Questions ]
a => [ Python Interview Questions ]
a => [ JavaScript Interview Questions ]
a => [ Angular Interview Questions ]
a => [ Selenium Interview Questions ]
a => [ Spring Boot Interview Questions ]
a => [ HR Interview Questions ]
a => [ C Programming Interview Questions ]
a => [ C++ Interview Questions ]
a => [ Data Structure Interview Questions ]
a => [ DBMS Interview Questions ]
a => [ HTML Interview Questions ]
a => [ IAS Interview Questions ]
a => [ Manual Testing Interview Questions ]
a => [ OOPs Interview Questions ]
a => [ .Net Interview Questions ]
a => [ C# Interview Questions ]
a => [ ReactJS Interview Questions ]
a => [ Networking Interview Questions ]
a => [ PHP Interview Questions ]
a => [ CSS Interview Questions ]
a => [ Node.js Interview Questions ]
a => [ Spring Interview Questions ]
a => [ Hibernate Interview Questions ]
a => [ AWS Interview Questions ]
a => [ Accounting Interview Questions ]
h2 => [ Learn Latest Tutorials ]
p => [ Splunk ]
p => [ SPSS ]
p => [ Swagger ]
p => [ Transact-SQL ]
p => [ Tumblr ]
p => [ ReactJS ]
p => [ Regex ]
p => [ Reinforcement Learning ]
p => [ R Programming ]
p => [ RxJS ]
p => [ React Native ]
p => [ Python Design Patterns ]
p => [ Python Pillow ]
p => [ Python Turtle ]
p => [ Keras ]
h2 => [ Preparation ]
p => [ Aptitude ]
p => [ Reasoning ]
p => [ Verbal Ability ]
p => [ Interview Questions ]
p => [ Company Questions ]
h2 => [ Trending Technologies ]
p => [ Artificial Intelligence ]
p => [ AWS ]
p => [ Selenium ]
p => [ Cloud Computing ]
p => [ Hadoop ]
p => [ ReactJS ]
p => [ Data Science ]
p => [ Angular 7 ]
p => [ Blockchain ]
p => [ Git ]
p => [ Machine Learning ]
p => [ DevOps ]
h2 => [ B.Tech / MCA ]
p => [ DBMS ]
p => [ Data Structures ]
p => [ DAA ]
p => [ Operating System ]
p => [ Computer Network ]
p => [ Compiler Design ]
p => [ Computer Organization ]
p => [ Discrete Mathematics ]
p => [ Ethical Hacking ]
p => [ Computer Graphics ]
p => [ Software Engineering ]
p => [ Web Technology ]
p => [ Cyber Security ]
p => [ Automata ]
p => [ C Programming ]
p => [ C++ ]
p => [ Java ]
p => [ .Net ]
p => [ Python ]
p => [ Programs ]
p => [ Control System ]
p => [ Data Mining ]
p => [ Data Warehouse ]
